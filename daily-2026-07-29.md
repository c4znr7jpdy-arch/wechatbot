# 2026-07-29 开发日报

## GemAI 每日签到修复

- 定位每日签到中 GemAI 返回未授权的问题，确认为原网页 session 已失效。
- 使用浏览器导出的最新请求信息更新 GemAI 登录 session，并将签到请求头对齐到当前个人资料页。
- 通过 AstrBot Python 3.13 环境和实际 `httpx` 客户端调用月度签到查询接口验证：HTTP 200，响应 `success: true`。
- 修复 `jiang_schedule` 热重载未刷新 `ai_plugin.checkin` 的模块缓存问题，插件版本升级至 1.1.3。
- `ai_plugin/ai_plugin/checkin.py` 已通过 Python 语法编译检查。
- 通过 AstrBot WebUI 热重载 `jiang_schedule`；运行日志确认 1.1.3 已加载，未来任务页确认每日签到任务保持启用，下次执行时间为 2026-07-30 08:30。
- 未手动触发签到 POST，未启动、停止或重启 AstrBot 与 `Py/main.py`。

## AngelHeart 插件适配

- 完成 `astrbot_plugin_angel_heart` 0.9.3 的依赖安装、配置生成和本地适配。
- 补入上游待合并修复：熟悉度超时配置读取，以及仅对连续相同纯文本触发复读判定；媒体消息和占位消息会重置复读状态。
- 将 AstrBot 错误提示拦截改为可配置项，默认关闭，避免插件静默吞掉真实故障信息。
- 使用 `openai_2/mimo-v2.5` 作为群聊分析和图片理解模型，补充“姜小妹”身份、称呼和回复策略；群聊上下文接管启用，私聊接管保持关闭。
- 关闭全局默认图片理解模型，避免 AstrBot 与 AngelHeart 对同一图片重复调用视觉模型；AngelHeart 仍使用自己的图片理解配置。
- 保留 `jiang_repeater` 为唯一复读处理器，关闭 AngelHeart 复读参与；开启 AngelHeart 密集讨论参与，避免重复回复冲突。
- LivingMemory 当前通过 `extra_user_content` 注入记忆，AngelHeart 重构群聊历史时不会清除此字段；Self Learning 的 LLM Hook 当前关闭，因此两者未发现直接冲突。
- 通过 WebUI 停用 `astrbot_plugin_proactive_chat`，未卸载；其调度任务和管理服务已正常结束。
- 修改文件通过语法编译和定向行为测试；热重载后日志确认 AngelHeart 正常初始化，未出现新的错误。
- 全程未启动、停止或重启 AstrBot 与 `Py/main.py`。

## AngelHeart 回复延迟与微信兼容复查

- 根据 11:30 的实际消息日志拆分耗时：AngelHeart 秘书模型约 23.7 秒、LivingMemory 召回约 2.2 秒、主回复模型约 10.7 秒，总回复约 34.7 秒；瓶颈主要是被呼唤前重复执行秘书模型。
- 新增“被呼唤时快速回复”：当配置已保证被呼唤必须响应时，直接构造参与决策并交给主模型，跳过不会改变结果的秘书模型调用。
- 修复微信内部身份提示误触发：桥接层注入的 `[系统身份提示：...]` 含“姜小妹”，此前会导致普通群消息被误判为呼唤；唤醒词判断现会先剔除该内部提示，但真实消息中的“姜小妹/小妹”仍可触发。
- 呼唤判定改为只检查当前事件：仅 @ 机器人、引用机器人或当前真实文本点名才算呼唤，避免旧总账中的 @ 状态串到下一条普通消息，@ 其他群友不会触发机器人。
- 微信 `xxx@chatroom` 群号不再调用仅适用于数字 QQ 群号的历史接口，直接回退 AstrBot 官方会话历史，消除 `int()` 转换失败。
- 恢复 AstrBot 命令唤醒前缀为 `/` 与 `\`；AngelHeart 对已识别的上游命令继续跳过，不清洗、不扣押。
- 复查表情包链路：meme_manager 本地路径仍保留 `sub_type=emoji`，微信桥仍通过 `11254` 发送；普通图片不会被标记为表情包。
- 新增微信兼容回归测试，覆盖双命令前缀、非数字群聊/私聊 ID 发送；连同表情包和引用图片测试共 8 项通过。AngelHeart 快速路径、身份提示过滤和历史回退定向测试通过，相关文件语法编译通过。
- 运行态验证：11:45:59 的普通群消息保持“不在场/不参与”，未再被内部身份提示误判为呼唤；11:37:51 的 meme_manager 表情包实际通过微信 `type=11254` 成功发送，而非普通图片接口。
- 11:38:13 运行日志显示 AstrBot 主任务收到取消并开始关闭；现有外部进程控制随后于 11:40:13 重新拉起 AstrBot。未手动执行启动、停止或重启命令，`Py/main.py` 的 Python 3.11 32-bit 进程始终保持运行。
- 11:45 通过 WebUI 热重载 AngelHeart，日志确认 0.9.3 与最终适配代码正常初始化。
- 明确 AngelHeart 的角色术语：插件内部“老板”指负责最终回复的前台主模型（姜小妹），“秘书”指判断是否接话的分析模型，“来访者”指群友；配置页保留“老板画像”比喻并补充清晰说明。现实中的唯一管理员是 `fengchenhao002`（祈、姜小妹的哥哥），该关系继续写入姜小妹画像和桥接层身份提示，但不与插件内部老板角色混淆。
