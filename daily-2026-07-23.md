# 2026-07-23 开发日报

## Grok 上游 503 与自动降级诊断

### 现象

- 16:55 至 16:59，默认模型 `jyjapi/grok-4.5` 连续返回 HTTP 503：`No healthy Grok OAuth account is currently available`。
- 当前配置会对单个模型最多重试 5 次，因此在切换后备模型前等待了约 4 分钟。
- 16:59:54 AstrBot 自动切换到 `openai_2/mimo-v2.5`，17:00:04 正常生成并发送回复；AstrBot 与转发解析流程均未崩溃。

### 诊断

- 当前默认聊天模型和默认图片理解模型均为 `jyjapi/grok-4.5`。
- 后备顺序为 `openai_2/mimo-v2.5`、`openai_2/mimo-v2.5-pro`、`jyjapi/grok-4.5`。
- 使用当前配置对上述三个模型执行最小请求，三者现均返回 HTTP 200，确认故障属于 Grok 上游账号池的短时波动，而非本地代码、密钥或转发聊天解析错误。
- 本次未修改模型顺序或重试次数，也未启动、停止、重启或热重载任何运行进程。

## 转发聊天记录中的 B 站专栏误解析

### 问题

- 转发聊天记录已成功展开 22 条消息，并成功下载 2 张图片；故障不在转发记录或图片解析。
- `jiang_douyin` 使用过宽的 B 站 URL 规则，把 `bilibili.com/read/cv...` 专栏误当成 BV 视频。
- 原规则使用 `\S+` 获取 URL，在转发事件的 JSON 文本中还会把字面量 `\n` 及下一条消息吞进 URL，最终触发 `Cannot extract BV ID` 并返回“链接解析失败”，截断原本的 AI 分析。

### 修改

- B 站长链接只匹配解析器实际支持的 BV 视频页；`/read/cv...` 专栏不再触发视频插件，继续交给普通 AI 对话分析。
- 保留 `b23.tv`、`b22.tv` 短链以及抖音、TikTok 链接支持。
- URL 字符范围排除反斜杠、引号及尖括号，使转发 JSON 中的字面量 `\n` 成为明确边界。
- `jiang_douyin` 的触发过滤器与共享 `extract_url()` 使用同一套规则，避免“先触发、后提取失败”的规则漂移。
- 新增 5 项链接识别回归测试，覆盖 B 站专栏排除、BV 视频、B 站短链、转发 JSON 换行边界和 TikTok。

### 验证

- 相关文件通过 AstrBot Python 3.13 `py_compile`。
- 5 项单元测试全部通过。
- 使用用户提供的 6314 字符真实报错日志回归，`extract_url()` 返回 `None`，不再误入视频解析流程。
- `git diff --check` 通过。
- 19:11 通过已登录的 AstrBot WebUI 热重载 `jiang_video_understanding`；日志确认旧处理器已移除并重新加载插件 1.0.0。
- 19:18 完成语境识别与口语化提示优化后再次热重载；日志确认新处理器加载成功，旧 schema v1 视频缓存验证为不会命中。
- 19:27 加入 MiMo 2.5 语义审校、综合回答兜底和 Bin/Zeus 指纹背景后再次热重载，平台日志确认插件加载成功。
- 19:34 加入 Grok 视觉失败后的 MiMo 全链路熔断并热重载，平台日志确认新处理器加载成功。
- 未启动、停止或重启 AstrBot 与 `Py/main.py`。

## 姜小妹插件能力自我认知

### 问题

- 用户询问群分析插件及报告模板时，模型虽然正确调用了 `capability_catalog`，但工具只返回少量可自然调用能力，没有包含通过 `/命令` 触发的插件。
- 模型因此无法得知已经启用的群分析等命令型插件，会错误回答“没听说过这个插件”。

### 修改

- `jiang_capability_router` 的能力目录新增 AstrBot 运行时命令读取，直接复用 `list_commands()`，不再依赖容易过期的手工命令清单。
- 能力目录现在明确区分：
  - AI 可直接自然调用的能力；
  - 需要用户发送 `/命令` 触发的插件能力。
- 命令型能力会展示插件名称、实际生效命令、用法说明和管理员权限。
- 查询支持中文模糊匹配；例如“群分析 模板”可定位群分析插件的 `/群分析`、`/设置模板`、`/查看模板` 等命令。
- 自动过滤停用命令和 AstrBot 内置保留命令，并使用当前运行时数据反映插件启停、命令改名及权限变化。
- 更新注入给模型的能力规则：询问插件、功能或命令用法时必须查询 `capability_catalog`；命令型能力不能因 `capability_invoke` 不支持而回答“不知道”。

### 验证

- 相关 Python 文件通过 `py_compile`。
- `jiang_capability_router` 的能力解析及网关测试共 25 项全部通过。
- 新增测试覆盖命令型群分析能力发现、权限展示、无关插件过滤、停用/保留命令过滤，以及工具读取运行时命令目录。

### 运行说明

- 本次未启动、停止或重启 AstrBot 与 `Py/main.py`。
- 修改需在用户自行重载 `jiang_capability_router` 插件或重启 AstrBot 后进入当前运行实例。

## 燕云与 B站最新动态

### 问题

- 自然语言请求“给我看看燕云动态”虽然能选中 `info.yanyun_dynamic`，但 `capability_invoke` 使用异步生成器返回，AstrBot 4.26.6 实际执行时被判定为“没有返回值”，用户收不到内容。
- 原实现默认展示多条动态，图文推送最多发送三张图；视频、专栏和纯文字动态没有统一的单图策略。

### 修改

- 燕云动态和通用 B站 UID 动态统一只输出真正最新的一条。
- 为避开空间置顶旧动态，先读取最近十条可用动态，再按发布时间选出最新项。
- 文本保留动态类型、标题或正文摘要、作者、时间与原文链接。
- 每次只附带一张相关图片：依次使用图文首图、视频封面、专栏封面，纯文字动态回退到作者头像。
- `/燕云` 命令、自然语言能力调用和定时 B站动态推送共用相同的单条单图规则。
- 将 `capability_invoke` 改为普通异步函数返回协议；带图片的动态结果由工具直接发送文本与图片，并在发送后清理临时图片。

### 验证

- 相关 Python 文件通过 `py_compile`。
- `jiang_capability_router` 相关测试共 32 项全部通过。
- 使用燕云官方 UID 实测成功取得当前最新专栏、生成简洁文本，并下载一张 4,068,418 字节的 PNG 封面。

### 运行说明

- 本次未启动、停止或重启 AstrBot 与 `Py/main.py`。
- 首次热重载时，`ai_plugin.bilibili_dynamic` 仍残留在 Python 模块缓存中，导致 `jiang_commands` 与 `jiang_capability_router` 导入新函数失败，`/燕云` 因此未注册并落入普通 AI 对话。
- 三个调用方现在都会显式刷新共享的 B站动态模块，避免插件热重载继续使用旧模块对象。
- 已通过 AstrBot WebUI 成功热重载 `jiang_capability_router`、`jiang_commands` 与 `jiang_schedule`；日志确认三者均加载成功，未重启 AstrBot 或 `Py/main.py`。

## B站动态订阅命令

### 问题

- `jiang_schedule` 的处理器入口只匹配 `\定时`，导致代码中已有的 `\订阅动态`、`\取消订阅动态` 和 `\订阅列表` 永远无法进入处理器。
- 插件热重载时 AstrBot 不一定再次调用异步 `initialize()`，新模块的定时回调发送上下文可能为空。

### 修改

- 对外命令统一使用正斜杠：`/订阅动态 UID [分钟]`、`/取消订阅动态 UID`、`/订阅列表` 及 `/定时...`。
- 入口正则现在直接覆盖定时管理和三种 B站订阅命令；旧反斜杠输入保留兼容，但所有提示只展示正斜杠。
- 插件构造阶段同步绑定回调上下文，确保热重载后新注册的动态轮询任务能够主动发送消息。
- 插件版本更新为 `1.1.1`。

### 验证

- 相关文件通过 `py_compile` 与 `git diff --check`。
- `jiang_capability_router` 与定时桥接相关测试共 36 项、4 个子测试全部通过。
- 已通过 AstrBot WebUI 热重载 `jiang_schedule`，后台显示启用版本 `1.1.1`，运行日志确认加载成功；未重启 AstrBot 或 `Py/main.py`。

### 燕云江湖路订阅配置

- 从微信服务群列表及历史发送记录确认，“燕云江湖路”的群 UUID 为 `53288922794@chatroom`，AstrBot 会话为 `aiocqhttp:GroupMessage:53288922794@chatroom`。
- 已为该群配置燕云官方 B站 UID `1567141152`，每 2 分钟检查一次。
- AstrBot“未来任务”页面已显示运行时任务 `bili_aiocqhttp:GroupMessage:53288922794@chatroom_1567141152`，状态启用并生成下一次执行时间。
- 首轮轮询已写入该群专属动态基线，未补发旧动态；后续只在作者发布新内容后推送最新一条和一张相关图片。

## 燕云动态旧内容误推修复

### 问题

- 15:08 和 15:10 分别误推了 7 月 21 日、7 月 22 日已经发布过的两篇燕云专栏。
- 原轮询状态只保存一个 `last_id`；当 B站接口某一轮返回旧缓存、当前窗口不包含该 ID 时，代码会把整页内容都当成新动态，并把基线倒退到旧内容。
- 下一轮接口恢复后，又会把较新的旧内容再次当作新动态，形成连续误推。

### 修改

- B站订阅状态升级为 `last_id + last_timestamp + seen_ids`，以发布时间和动态 ID 组成只能前进的高水位。
- 接口最新结果早于已保存高水位时直接忽略，禁止状态倒退。
- 旧版字符串状态若未出现在当前接口窗口中，保留原基线并跳过本轮，避免不确定情况下补发历史内容。
- 每次轮询取 10 条内容并保留最近 30 个动态 ID，增强置顶、换序和短期接口缓存波动下的去重。
- 同一进程内串行执行 B站轮询，状态文件改为临时文件替换写入，避免并发读写覆盖。
- 状态仅在消息确认发送成功后推进；发送失败会保留原高水位供下轮重试。
- `jiang_schedule` 版本更新为 `1.1.2`。

### 验证

- `jiang_schedule/main.py` 通过 AstrBot Python 3.13 `py_compile`。
- 状态函数回归 5 项通过：旧状态迁移、缺失基线保护、旧缓存倒退识别、真实新动态前进和最近 ID 容量限制。
- 回调回归 4 项通过：旧缓存不推送、旧基线缺失不覆盖、发送失败不推进、发送成功后提交新高水位。
- 未启动、停止或重启 AstrBot 与 `Py/main.py`。
- AstrBot WebUI 当前未登录，本次未代填管理凭据、未热重载插件；修复将在下一次手动重载 `jiang_schedule` 后生效。

## AstrBot 后备模型残留与自学习筛选连接诊断

### 修改

- 从 `provider_settings.fallback_chat_models` 删除已不存在的 `openai/grok-4.20-0309-non-reasoning` 和 `openai_3/grok-4.3-low`。
- 通过已登录的 AstrBot WebUI 保存配置，使运行时与磁盘配置同步；当前后备模型为 `openai_2/mimo-v2.5`、`openai_2/mimo-v2.5-pro`、`jyjapi/grok-4.5`。

### 诊断

- 16:00:07 的“筛选模型调用失败”来自 `astrbot_plugin_self_learning`，失败模型为 `openai_2/mimo-v2.5-pro`，用途是对学习样本做筛选，不是主对话模型调用。
- 该请求在 15:59:47 至 15:59:58 共记录 5 次连接重试，最终于 16:00:07 失败；同一轮主对话仍继续执行工具并回复，未造成主聊天中断。
- 当前 DNS、TCP 443、鉴权后的 `/v1/models` 和最小聊天请求均验证成功，`mimo-v2.5-pro` 存在且接口返回 HTTP 200，因此判断为当时的临时网络或上游连接波动。
- 09:00 至 10:02 曾出现多次 `401 Invalid API Key`，但当前密钥和接口已可正常鉴权，与 16:00:07 的连接错误不是同一种故障。
- 日志显示主 Agent 曾通过文件读取工具读取完整 `data/cmd_config.json`；该文件含供应商密钥，存在密钥内容被发送给远端模型的风险，建议轮换相关密钥并限制工具读取敏感配置。

### 验证

- `data/cmd_config.json` 可正常解析，后备列表数量为 3，两条失效 ID 均不存在。
- AstrBot WebUI 显示保存成功，运行页面不再包含两条失效 ID。
- 自学习插件当前仍配置并成功初始化 3 个 `openai_2/mimo-v2.5-pro` 角色：筛选、提炼、强化。
- 清理后日志未再出现两条失效 Provider 的警告，也未再出现新的“筛选模型调用失败”。
- 未启动、停止或重启 AstrBot 与 `Py/main.py`。

## 微信短视频理解插件

### 现状与接入点

- 历史日志确认微信 DLL 对收到的视频使用 `11051` 回调，`raw_msg` 中的 `<videomsg>` 提供 `cdnvideourl`、`aeskey`、大小和播放时长。
- 原桥接层只处理文本、语音和引用消息，虽然支持机器人向微信发送视频，但没有下载并向 AstrBot 转发收到的视频。
- AstrBot 能解析 OneBot `video` 消息段，但默认 Agent 只把本地视频路径写进提示词，不会自动读取画面和音轨。

### 修改

- 新增 `jiang_video_understanding` 插件：
  - 使用 FFprobe 校验视频时长、大小、分辨率和音轨；
  - 使用 FFmpeg 并行抽取均匀关键帧和 16kHz 单声道音轨；
  - 复用 AstrBot 图片描述模型进行关键帧时间线分析；
  - 优先使用当前 STT Provider，未配置时自动寻找支持 `audio` 的聊天模型转写音轨；
  - 并行完成画面理解和音频转写，再由当前会话模型综合回答；
  - 相同视频缓存画面时间线和转写，后续可针对不同问题重新综合，不重复预处理；
  - 对媒体内提示词进行不可信数据隔离，避免画面或音频指令覆盖系统要求；
  - 默认限制 5 分钟、100MB、10 帧、最多 2 个并发任务，缓存 6 小时。
- 使用方式：
  - 私聊直接发送视频，自动回复处理状态并总结；
  - 群聊以“引用目标视频并 @机器人 说分析视频”为主流程；
  - 也支持发送视频后使用 `/总结视频` 或 `@机器人 看看刚才的视频`。
- `Py/main.py` 新增 `11051` 处理：
  - 解析视频 CDN 参数；
  - 在后台线程调用 `11230`、`file_type=4` 下载，避免阻塞 DLL 回调线程；
  - 下载成功后构造 OneBot V11 `video` 消息段交给 AstrBot；
  - 原始视频优先但受大小限制，标准视频作为稳定回退；
  - 桥接视频缓存保留 12 小时并自动清理。
- `11061` 引用消息新增 `refermsg.type=43` 专用路径：
  - 使用 `refermsg.svrid` 精确关联被引用视频，不按群聊“最新视频”猜测；
  - 优先复用 `11051` 已下载的同消息 ID 缓存，未命中才解析引用 XML 的 CDN 参数；
  - 同一视频使用下载锁去重，避免刚发视频就引用时产生两次 CDN 下载；
  - 将当前 @、被引用的视频文件和用户问题放进同一个 OneBot 事件；
  - 群聊引用视频但没有 @机器人、也没有命令前缀时保持静默。
- 修复群聊自然问法未触发：
  - 同一事件中同时存在 `Video` 和对机器人的 `At` 时，直接视为明确的视频分析请求；
  - 不再要求用户必须命中“分析/总结”等固定关键词，因此“这个视频说的什么意思”也能触发；
  - 视频处理器优先级提高，避免被通用消息处理链抢先截断。
- 修复只回复“正在看”但不继续解析：
  - 根因是调用 `stop_event()` 后先 `yield` 状态消息，AstrBot 会在第一个 `yield` 后关闭该处理器；
  - 状态提示改为主动发送，最终解析结果作为处理器唯一一次 `yield`，确保解析逻辑实际执行；
  - 短视频按时长动态减少关键帧，9 秒视频默认只取约 3 帧，而非固定 10 帧；
  - 增加预处理、画面/音频分析和总耗时日志；
  - 视觉、音频、综合模型分别设置超时，单个任务总超时默认 90 秒，避免永久挂起。
- 改进视频语义理解与回复风格：
  - 原视觉提示过度强调“只客观描述”，导致 BLG 球衣、电竞选手等身份线索被忽略，只输出机械画面时间线；
  - 视觉分析现会优先读取球衣、队标、姓名、比赛 UI 与字幕，并结合公开常识识别电竞选手、战队、游戏和网络语境；
  - 增加人物互动、反差和梗点判断，证据充分时直接给出人物身份，不确定时说明判断依据；
  - 9.5 秒短视频由 3 帧提高为 6 帧，在控制耗时的同时保留更多表情与身份线索；
  - 默认问题改为解释视频真正含义，最终回复限定为 2～5 句自然口语，不再默认使用 Markdown 标题、项目符号或时间线；
  - 解析缓存升级到 schema v2，旧版机械描述缓存会自动失效并重新分析。
- 增加电竞恶搞语境与 MiMo 兜底：
  - 视觉分析强制区分真实关系和剪辑叙事，识别“把赛场对手剪成恩爱/CP”的粉丝二创；
  - 主视觉输出泛化人物、缺少人物身份/视频类型/梗点/判断把握，或置信度为中低时，自动调用 `openai_2/mimo-v2.5` 对同一组关键帧二次审校；
  - 主视觉模型失败时直接切换 MiMo 2.5，主综合模型失败或返回空内容时同样由 MiMo 2.5 生成最终回答；
  - 对同时识别出 Bin 和 Zeus 的视频注入已确认背景：两人是 LOL 赛场对手，相关恩爱/CP内容属于网友利用“世界第一上单”与被 Zeus 击败反差制作的恶搞二创；
  - 新增按视频 SHA-256 隔离的 `context_hints.json`，本次视频已写入用户确认背景，只对完全相同文件生效；
  - 缓存升级到 schema v3，避免继续使用没有语义审校的旧缓存。
- 增加 Grok 失败后的 MiMo 全链路熔断：
  - 主视觉 Grok 失败后，不再只让 MiMo 接管关键帧，而是将本次任务的最终综合回答也固定到 `openai_2/mimo-v2.5`；
  - 缓存记录熔断后的首选综合模型，相同视频命中缓存时仍保持 MiMo 全链路，不会重新回到 Grok；
  - 缓存升级到 schema v4，旧缓存自动失效；
  - 回归测试验证模型调用顺序严格为 `Grok失败 → MiMo视觉 → MiMo最终回答`。
- `.gitignore` 放行新插件源码，继续忽略其 `__pycache__`。

### 验证

- `Py/main.py` 使用强制的 Python 3.11 32-bit 通过 `py_compile`。
- 视频插件使用 AstrBot Python 3.13 通过 `py_compile` 和导入验证。
- 视频插件 14 项测试全部通过，新增覆盖 Grok 视觉失败后 MiMo 接管剩余全链路。
- 微信视频 XML、双层转义引用视频、精确引用事件组合和异常 XML 降级测试通过；`Py/test` 共 13 项测试全部通过。
- `git diff --check` 通过。
- 未启动、停止、重启 AstrBot 或 `Py/main.py`，也未热重载插件。

## LivingMemory 嵌入服务超时降级

### 问题

- 图片消息于 19:24:02 进入记忆召回后，当前 `openai_embedding` 的 GemAI `/embeddings` 请求连续读取超时，OpenAI SDK 重试累计约 64 秒，导致正常的图片理解回复被记忆插件阻塞。
- 文档向量检索虽然已有异常回退，但没有独立的路由时限；图记忆向量检索也没有异常隔离，任一路报错都会使整次双路召回失败。
- 实测同一代理环境下 GemAI 端点 8 秒仍超时，备用 SiliconFlow 嵌入端点约 0.63 秒返回 HTTP 200。

### 修改

- 为文档向量检索和图向量检索增加可配置的 `vector_search_timeout_seconds`，默认 5 秒。
- 向量检索超时后，文档路自动退化为 BM25，图路自动退化为图关键词检索，继续生成正常回复。
- 文档路与图路增加顶层异常隔离，单路故障不再拖垮另一条召回路径。
- 在 LivingMemory 配置 schema、校验模型和当前插件配置中补充该超时项。

### 验证

- 新增文档向量超时回退和图向量超时回退测试；AstrBot 虚拟环境未安装 `pytest`，因此未运行 pytest 套件。
- 使用 AstrBot Python 3.13 执行等价异步回归，文档路与图路都在约 0.10 秒的测试时限后取消慢向量任务并正常返回关键词结果；相关 Python 文件通过 `py_compile`，两个 JSON 配置通过解析校验。
- 未启动、停止、重启 AstrBot 或 `Py/main.py`，也未热重载插件。
