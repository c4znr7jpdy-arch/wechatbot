# 2026-07-20 开发记录

## 移动端活动报名排版

- 活动选择由紧凑文字节点改为完整活动卡片，突出活动名称。
- 卡片独立展示活动类型、日期、时间和报名人数。
- 选中卡片显示“当前选择”及金色边框、侧边强调线。
- 报名按钮动态显示当前活动名称，例如“报名《双10》”。
- 移动端使用单列卡片和更大的活动标题，提高微信内置浏览器中的辨识度。

## 生图意图触发修复

- 补充裸“生图”及“/生图，…”等自然指令的插件预过滤与意图识别，避免请求落入普通 LLM。
- 将“生图”加入提示词提取关键字，防止生成提示词中残留命令文本。
- 修复生成器注册表重建后的陈旧引用，主图片模型失败时可实际回退到 GPT1 / MiniMax。

## “图来”随机图片网络容错

- 移除 `jiang_commands` 对本机代理 `127.0.0.1:7890` 的硬编码，默认自动使用当前环境或系统代理，并支持通过 `JIANG_RANDOM_IMAGE_PROXY` 单独覆盖。
- 将当前可访问的 `https://boudoir.ortlinde.com/random` 恢复为主图片源；主源故障、被 Cloudflare 拦截或代理不可用时，自动尝试直连及全年龄备用图片源。
- 增加 HTTP 状态、`Content-Type` 和内容长度联合校验，避免把 Cloudflare HTML 错误页当成图片发送。
- 将重试参数收敛为两轮、单次 15 秒，并把可预期的网络失败记录为简短警告，避免输出整段代理异常堆栈。

## “图来”修复验证

- `astrbot_venv\Scripts\python.exe -m py_compile data\plugins\jiang_commands\main.py` 通过。
- 实际网络验证在主源返回 502、直连返回 403 后自动切换备用源，成功取得 `image/jpeg`（564978 字节）。
- 模拟 `127.0.0.1:7890` 代理拒绝连接，确认请求按“代理 → 直连 → 下一图片源”顺序降级并返回有效图片。

## 生图后端切换至 Sub2API

- 将 `.env.prod` 中的 GPT 图像配置替换为 `https://sub.xiuxianjyj.xin/v1` 和 `gpt-image-2`，旧 `GPT2_IMAGE_*` / GRS 配置已移除。
- 活动 `jiang_image` 插件不再读取或注册旧 GPT2 后端；`gpt` 模型命令现在指向新的 OpenAI-compatible GPT 后端，MiniMax 仅保留为故障兜底。
- 文生图和图生图端点均兼容 base URL 已含或未含 `/v1` 的写法，避免请求落到 `/v1/v1/images/...`。
- 已通过 Python 静态编译、环境变量校验、模块加载以及 generations/edits 端点归一化断言。

## GPT 生图断连修复

- 日志确认新 Sub2API 曾在同一配置下成功出图，后续失败发生在长请求阶段，并非 Key、模型或 API 路径错误。
- 发现 AstrBot 进程继承了指向 `127.0.0.1:7892` 的系统代理；GPT 图片请求经过本机代理时会出现 `Server disconnected without sending a response`。
- GPT 文生图和图生图客户端改为 `trust_env=False` 强制直连，不影响其他下载与业务后端的代理行为；图生图超时同步调整为 300 秒。
- 使用相同 URL、Key 和 `gpt-image-2` 绕过代理真实请求成功，HTTP 200，约 39.2 秒返回一张图片。

## 活动取消报名命令

- `jiang_raid_signup` 新增“取消报名活动名”命令，支持 `取消报名双10`、`取消报名 双10` 及斜杠前缀写法。
- 盛世网站新增 `POST /api/bot/raid/cancel` 令牌鉴权接口，可同时取消站内成员和访客报名，并返回取消状态与最新报名人数。
- 取消成功后发送活动链接卡片；未报名、活动不存在和卡片发送失败均有明确回退提示。
- 插件版本升级至 2.3.0；网站全量接口测试通过，插件 19 项单元测试和 Python 编译检查通过。
- 生产网站已备份原服务文件并部署新接口，受控重载 `shengshi` PM2 服务后保持 `online`；线上验证未授权请求返回 401，授权的不存在活动请求返回 404，未写入测试报名数据。
- 通过已登录的 AstrBot WebUI 仅重载 `jiang_raid_signup`，页面与运行日志均确认已载入 2.3.0；AstrBot 主进程、Python 子进程和 `Py/main.py` PID 保持不变。
- 未启动、停止、重启或热重载 AstrBot 与 `Py/main.py`。

## “报名”日历卡片优化

- 群里只发送“报名”时，`jiang_raid_signup` 会实时请求网站的可报名活动列表，卡片小字显示“当前最近活动：《活动名》”。
- 存在最近活动时，点击卡片直接进入该活动的 `event.html?id=...` 详情页；暂无活动或查询失败时才回退到活动日历。
- 实网查询确认当前最近可报名活动为《双10》；插件 21 项单元测试及 Python 编译检查通过。
- 插件版本升级至 2.3.1 并通过 WebUI 仅重载 `jiang_raid_signup`；运行日志确认已加载 2.3.1，AstrBot 和 `Py/main.py` 进程 PID 未变。

## 活动名称括号样式统一

- 活动报名链路中的活动名由书名号 `《》` 统一替换为方头括号 `【】`，覆盖报名、重复报名、取消报名、未报名、开始提醒、最近活动卡片和文本回退。
- 盛世网站的机器人报名/取消返回文案及管理页金山脚本提示同步使用 `【】`，避免不同入口样式不一致。
- 网站全量测试、插件 21 项单元测试和 Python 编译检查通过，活动报名相关代码中已无 `《》` 残留。
- 生产网站已备份并部署新文案，`shengshi` 服务保持在线；`jiang_raid_signup` 升级至 2.3.2 并单独重载成功。

## 金山文档名单定时同步

- `jiang_raid_signup` 新增 AirScript Webhook 调度，按北京时间每周一 11:00–19:30、从 11:00 起每 10 分钟执行一次。
- Webhook 地址和 AirScript Token 存放在运行时插件配置中，日志不打印令牌；调度具备同一分钟去重和插件卸载任务清理。
- 新增时间窗口、10 分钟对齐及 Webhook 请求头/请求体测试；插件共 14 项测试通过，Python 静态编译通过。
- 首个 AirScript Token 调用返回 `ApiTokenNotExists`；更换有效令牌后，直连及机器人实际使用的 `127.0.0.1:7892` 代理均返回 `status=finished`。
- 已启用 `enable_kdocs_sync` 并通过 AstrBot 插件接口热更新 `jiang_raid_signup 2.2.0`；插件保持激活，未重启 AstrBot 或微信注入层。

## 网站报名反向同步金山文档

- 网站新增受令牌保护的本周活动名单读取接口；网站和机器人报名成功后可通过 AirScript Webhook 请求刷新表格。
- AirScript 增加明确的 `website_to_sheet` 调用方向：该方向只读取网站并覆盖 `B3:B12`，不会回调网站写入接口；普通手动/定时调用仍只执行“表格 → 网站”，避免循环。
- 线上网站已部署反向同步代码与 Webhook 配置，并保留原文件和 `.env` 备份；新接口已正确返回本周“双10”及当前 8 人名单。
- WPS 云端仍需覆盖保存新版脚本，因此线上 `KDOCS_WEBSITE_TO_SHEET_ENABLED` 暂时为 `false`；重复报名验证确认当前不会误触发旧脚本。
- 用户覆盖保存新版 AirScript 后，线上开关已调整为 `true` 并重载 `shengshi`；首次 `website_to_sheet` 调用完成。
- 使用既有成员进行无新增数据的重复报名验证：网站保持 8 人，返回 `kdocsTriggered=true`、`kdocsStatus=finished`，确认网站报名入口会触发金山同步。

## 取消报名同步修复

- 网站机器人 `/api/bot/raid/cancel` 在实际取消成功后调用同一 `website_to_sheet` Webhook；重复取消或名单未变化时返回 `reason=unchanged`，不会产生无效同步。
- 本地网站测试、Node 语法检查和完整测试套件通过，线上仅更新 `server/routes/bot.js` 并重载 `shengshi`。
- 线上使用临时测试成员完成“报名 → 金山同步 → 取消 → 金山同步”闭环验证，两次均返回 `status=finished`；测试成员已清理，未留在最终名单中。

## 金山文档小程序卡片回放验证

- 确认 DLL 消息类型 `11056` 为微信小程序消息，恢复并解析了 14:46:52 收到的“新百业本”金山文档卡片完整 `raw_msg` XML。
- 卡片为 `<appmsg type=33>`，小程序原始用户名为 `gh_c90b888d5c93@app`，页面对应 `https://www.kdocs.cn/l/ctAJaTopuHix`。
- 将收到的外层 `<msg>` 规范化为内层 `<appmsg>` 后，通过既有 `11214` 原始 XML 通道向来源群 `51632940287@chatroom` 仅发送一次；HTTP 控制接口返回 `ok=true`，DLL 回执 `baseResponse.ret=0`，新消息 ID 为 `1122048535989514865`。
- 未启动、停止或重启 AstrBot 与 `Py/main.py`。

## 新活动创建群通知

- 盛世网站新增新活动通知队列表及机器人查询/确认接口；活动创建后会进入待通知状态，群卡片发送成功后才持久化确认为已通知。
- 首次迁移会把已有活动预标记为已通知，生产库现有 2 条活动均已正确初始化，部署后待通知数为 0，不会补发历史活动。
- `jiang_raid_signup` 每 30 秒查询一次新活动，并向配置的活动群发送详情卡片；标题格式为“姜玉杰创建了【活动名】活动🎉”，小字展示活动时间和报名人数，点击直达对应活动详情页。
- 插件版本升级至 2.4.0；网站完整测试、Node 语法检查、插件 23 项单元测试和 Python 静态编译均通过。
- 生产网站部署前备份为 `/var/www/shengshi/backups/event-announcement-before-20260720-145206-CST.tar.gz`，部署后迁移 10 已应用，授权接口返回 0 条待通知，未授权请求返回 401，PM2 服务保持 `online`。
- 通过 AstrBot WebUI 仅热重载 `jiang_raid_signup`，页面确认加载 2.4.0；AstrBot PID 41632/41672 与 `Py/main.py` PID 9932 均保持不变。

## 新活动双卡片通知

- 新活动通知升级为两条连续消息：先发送原有活动详情卡片，再发送固定的“新百业本”金山文档小程序卡片；第一条的标题、小字和活动详情链接保持不变。
- 金山卡片复用已验证成功的 `<appmsg type=33>` XML，通过本机微信注入层 `http://127.0.0.1:18766/send_raw_xml` 和 `11214` 原始 XML 通道发送，页面固定指向 `sid=ctAJaTopuHix`。
- 只有两条消息都发送成功后才向网站确认本次新活动通知；金山卡片失败时会保留第一条的运行时发送状态，后续只重试第二条，避免当前插件运行期间重复发送活动卡片。
- 固定卡片 XML 独立保存为 `data/plugins/jiang_raid_signup/assets/kdocs-mini-program-card.xml`，运行配置新增双卡片开关和本机控制接口地址。
- 插件测试增至 28 项，Python 静态编译、XML/JSON 解析及本机控制接口健康检查均通过；健康接口返回 `ok=true, connected=true`，验证过程未向群聊发送测试消息。
- `jiang_raid_signup` 已通过 WebUI 单独热重载至 2.4.1；AstrBot PID 41632/41672 与 `Py/main.py` PID 9932 保持不变，线上当前无待通知活动。

## 管理后台活动名单维护

- 盛世网站“活动管理”列表新增“名单管理”入口，管理员可展开指定活动的完整报名名单，输入站内成员昵称或访客名称进行添加，并可按报名记录精确删除。
- 新增受管理员登录态保护的活动名单查询、添加和删除接口；未登录访问返回 401，重复添加、满 10 人、无效记录等情况均有明确错误提示。
- 每次实际添加或删除成功后都会调用既有 `website_to_sheet` 金山反向同步；界面区分“同步成功”“非当前金山活动”“同步未启用”和“同步请求失败”等状态，避免把网站保存成功误报为整体失败。
- 管理界面支持站内成员昵称建议、访客直接录入、10 人容量显示与满员禁用添加，移动端自动切换为纵向输入布局。
- 网站完整测试通过，覆盖管理员鉴权、站内成员添加、访客添加、重复拦截、两类报名记录精确删除和同步调用结果；服务端与前端 JavaScript 语法检查通过。
- 生产部署前备份为 `/var/www/shengshi/backups/admin-roster-before-20260720-160244-CST.tar.gz`，部署文件哈希一致，`shengshi` PM2 服务保持 `online`。
- 线上只读验证确认新页面与脚本已生效、未登录名单接口返回 401、当前【双10】名单保持 10/10；未为验收修改真实名单或金山表格。
- 修复部分浏览器命中旧 `admin.html`、同时加载新版 `admin.js` 时点击“名单管理”出现 `Cannot read properties of null (reading 'style')`：脚本现在会在名单容器缺失时自动创建，静态脚本版本升级为 `202607212` 强制刷新缓存。修复已完成测试并部署，线上响应已确认包含容器自修复逻辑。

## 活动开始提醒卡片手动预览

- 按用户要求向 `51632940287@chatroom` 仅发送一次【双10】提前 10 分钟提醒卡片，使用线上实际数据：开始时间 `2026-07-20 19:30`、当前报名 `10/10`，点击跳转活动 ID 5 的详情页。
- 本次仅用于查看卡片效果，未发送金山小程序卡片，也未写入正式提醒确认记录；微信注入层返回发送成功。

## 活动开始提醒卡片图标与时间优化

- 正式提醒卡片的小字改为仅显示具体时间，例如 `开始时间：19:30`，不再展示年月日；新增回归断言确保活动日期不会重新混入提醒文案。
- 确认首次手动预览缺图是因为走了 `11214` 原始 XML 通道，微信不会下载普通 `thumburl`；正式提醒继续使用 AstrBot `Share` 链路，由微信注入层转换为 `11039` 链接卡片并主动读取 `image_url`。
- 通过正式链路向 `51632940287@chatroom` 仅补发一次修正预览。微信注入层记录标题 `【双10】即将开始`、时间 `19:30`、报名 `10/10`、活动 ID 5 链接及 `sheng-card-icon.jpg`，`11039` 返回发送成功；未发送金山卡片，也未确认正式提醒队列。
- 一次性预览触发代码与标记已立即移除，最终插件不包含测试入口；插件版本升级为 2.5.1，33 项测试、Python 编译和差异检查全部通过，并已通过 WebUI 单独热重载。
- 未启动、停止或重启 AstrBot 与 `Py/main.py`。

## 活动开始后封存与清空金山名单

- 金山同步脚本新增 `archive_and_clear` 方向：先把 `B3:B12` 最终名单同步到网站，再封存本周同名活动；仅在封存接口成功后清空文档人员，任何同步或封存失败都保留表格名单。
- 网站新增本周活动 `ensure` 与 `archive` 接口。空白的新周表格只负责确保本周活动存在，不会把空名单反向覆盖到网站；封存将活动状态改为 `ended`，网站成员及访客报名历史完整保留。
- 封存接口严格校验中国时区的活动开始时间，开始前返回 HTTP 409；封存后活动不可继续报名，从而避免表格清空后产生新报名或双向同步循环。
- `jiang_raid_signup` 仅在周一同步结束时刻（当前为 19:30）向 AirScript 发送封存清空指令，其余 10 分钟任务保持普通名单同步；插件版本准备升级为 2.5.0。
- 网站代码已备份到 `/var/www/shengshi/.codex-backup-archive-20260720` 并部署，PM2 受控重载后健康检查正常。线上开始前验证返回 409，活动 ID 5 仍为 `upcoming` 且保留 10 人名单。
- 验证时因 PowerShell 请求体编码产生的一条空白乱码测试活动（ID 6）已在确认无成员/访客后精确删除；删除前数据库备份为 `data/backups/guild-before-codex-event6-cleanup-2026-07-20.db`，真实活动未受影响。
- 网站完整测试通过；机器人插件 Python 编译及 30 项单元测试通过。机器人插件尚未热重载，需先在 WPS 保存新版 AirScript，避免 19:30 云端仍执行旧逻辑。
- 用户保存新版 AirScript 后，以 `website_to_sheet` 方向执行云端脚本返回 `status=finished`、无错误；随后仅热重载 `jiang_raid_signup`，Dashboard 确认插件版本 2.5.0 且 `activated=true`。未重启 AstrBot 或微信注入进程，19:30 自动封存并清空逻辑现已生效。

## 转发聊天记录文字与多图理解

- 定位到微信引用合并转发卡片时会把原始 `recorditem/datalist` 裁成 `count=0`，只保留文字摘要和 `[图片]` 占位符；旧实现因此把整段转义 XML 送给模型，既难读也没有真实图片。
- `Py/main.py` 现可解析直接转发和被引用的 `type=19` 多层 XML，按发送人及原顺序还原聊天文字、图片编号和常见非文本消息类型。
- 新增最多 50 条的完整转发卡片持久缓存；引用消息通过 `refermsg.svrid` 回查原卡，避免微信精简引用副本丢失逐条数据，进程重启后缓存仍可继续使用。
- 首次实测发现微信 4.x 的完整记录把文字放在 `datadesc`，图片同时提供 `cdndataurl/cdndatakey/filetype=1` 原图参数与缩略图参数；已补齐全部文字与时间提取，并改为优先使用成对原图参数，避免缩略图误用 `file_type=3` 导致 DLL 返回 `-5103017`。
- 后台下载成功后以带编号的多图片段交给 AstrBot VLM；默认最多读取前 8 张，可由 `FORWARD_RECORD_MAX_IMAGES` 调整，下载失败时会明确要求模型不要猜图。
- 使用规定的 Python 3.11 32-bit 完成静态编译及 5 项离线测试，覆盖完整文字/多图解析、微信 4.x 原图参数优先级、精简引用摘要、CDN 字段多层转义和 `svrid` 完整缓存回查；真实 16 条问题记录现已还原全部 10 条文字、6 张图片占位及原始时间顺序，并为每张图生成匹配的原图/缩略图下载候选。
- 未启动、停止或重启 AstrBot 与 `Py/main.py`；代码需在用户授权下重启微信注入层后生效。

## 盛世活动报名接入自然语言能力网关

- 将 `jiang_raid_signup` v2.5.0 接入 `jiang_capability_router`，新增最近可报名活动查询、明确报名和明确取消报名三项能力。
- 自然语言报名继续复用原插件实例、群白名单、群备注、网站令牌与接口配置；未授权群和私聊不会产生写请求。
- 为报名与取消报名增加确定性写操作边界：只有当前消息明确包含“报名/报上”或“取消报名/撤销报名/退出活动”才可执行；“怎么报名、能报名吗、有什么活动”等疑问只查询或提示，不会写入。
- 查询最近活动后会记录活动名称，后续明确说“给我报上”或“取消报名”时可继承该活动；当前消息中明确给出的新活动名始终覆盖历史参数。
- 能力目录现在列出 `info.raid_activity`、`action.raid_signup`、`action.raid_cancel_signup`；能力网关 24 项、报名插件 33 项测试全部通过，相关 Python 文件通过编译检查，测试未调用真实报名接口。
- 仅热更新 `jiang_raid_signup` 与 `jiang_capability_router`，两项插件均为 `activated=true`，热更新日志无错误，6185/6199 继续监听；未启动、停止或重启 AstrBot 与 `Py/main.py`。

## 群昵称通知入群误报与头像修复

- 根据 `Py/wechat_service.log` 还原 16:54 事件时序：机器人入群时 DLL 先推送包含 55 名成员的 `11200` 全量快照，旧逻辑未将其写入基线；随后单成员资料事件被拿来与空缓存比较，因而误发“未设置 → 当前群昵称”卡片，并非历史改名通知积压。
- `Py/main.py` 现在把多成员 `11200` 明确视为当前资料基线；单成员事件只有在已有有效昵称/群昵称基线时才允许生成修改通知，空占位和首次资料补全只更新缓存并触发刷新。
- 单成员事件携带的新资料会覆盖刷新结果中的对应字段并写回缓存，确保机器人在线且已有基线后，真实改名仍按事件实时比较和通知。
- 全量成员快照缺少头像时不再清空已解析的头像 URL，并兼容 `avatar_url`、`head_img_url`、`small_head`、`big_head` 等常见 DLL 字段名。
- `jiang_group_notice` 的改名卡片在事件没有头像时新增 `get_user_info`（11029）补查；入群卡片的群成员查询也增加同样的二级回退，避免直接使用默认链接图。
- 新增 4 项离线回归测试，覆盖全量快照基线、空占位抑制、头像保留和头像字段归一化；规定的 Python 3.11 32-bit 下与转发记录测试合计 9 项全部通过，AstrBot Python 3.13 插件编译及改名卡片头像补查模拟通过。
- 未启动、停止、重启或热重载 AstrBot 与 `Py/main.py`；桥接层修复需用户授权后重启 `Py/main.py` 才会生效，卡片插件需在 AstrBot 中重新加载后生效。
