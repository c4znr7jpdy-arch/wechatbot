# 2026-07-14 开发记录

## 活动开始提醒

- 管理员保存活动时必须填写活动日期和开始时间。
- 更新管理页脚本版本号，绕过 Nginx 的 immutable 静态缓存，确保二次确认逻辑立即生效。
- 点击保存后进行二次确认，明确展示活动名称、完整开始时间及“提前 10 分钟发送群提醒”。
- 新增活动开始提醒记录表，活动提醒发送成功后持久化防重。
- 管理员修改活动日期或开始时间时自动清除该活动旧提醒记录，确保改期后仍会按新时间通知。
- 网站机器人接口提供待提醒活动查询和发送成功确认接口，接口继续使用机器人令牌鉴权。
- AstrBot 报名插件每 30 秒检查一次待提醒活动，在开始前 10 分钟向报名群白名单发送链接卡片。
- 提醒卡片显示“距离《活动名》开始还剩 N 分钟”、开始时间及当前报名人数，并链接到活动详情页。

## 米游社插件命令与扫码登录修复

- 定位到微信注入层的“系统身份提示”位于用户命令之前，导致 AstrBot 标准命令过滤器无法识别米游社命令。
- 新增米游社统一命令路由，剥离身份提示并兼容 `/`、`\\` 两种前缀；支持登录、签到、任务、地址、兑换、商品、便签、设置、Cookies 导出及帮助命令。
- 命令命中后停止后续 AI 流程，避免插件结果和普通 AI 回复重复发送。
- 登录二维码改为通过 OneBot 使用 Base64 图片直接发送，规避临时图片在异步发送前被删除的问题。
- 保留 AstrBot 图片结果回退路径，二维码临时文件由事件生命周期统一清理。
- 完成 Python 语法检查、命令路由/二维码编码冒烟检查，并确认米游社二维码接口当前可正常返回二维码数据。

## 运行进程控制约束

- 按用户要求关闭 AstrBot，保留 32 位微信注入进程 `Py/main.py` 运行。
- 在 `AGENTS.md` 增加硬性约束：未经用户在当前请求中明确授权，不得启动、停止或重启 AstrBot 与 `Py/main.py`，停止后也不得自动拉起。
## 米游社扫码登录确认报 JSON 错误

- 现象：二维码可被米游社 App 正常识别，但进入“未定事件簿”确认页后提示 `decode err response body error: unexpected end of JSON input`。
- 原因：持久化配置 `data/nonebot-plugin-mystool/configV2.json` 仍使用旧的 `game_token_app_id = "2"`，覆盖了当前代码中的默认值 `"4"`，因此生成了未定事件簿登录二维码。
- 修复：将运行时配置的 `game_token_app_id` 改为 `"4"`，后续生成原神登录二维码。
- 按用户要求，没有启动或重启 AstrBot，也没有停止 `Py/main.py`；配置将在用户下次手动启动 AstrBot 后生效。

## 米游社扫码接口代理超时修复

- AstrBot 日志显示 `fetch_game_token_qrcode` 抛出 `httpx.ConnectTimeout`，调用栈经过 `httpcore._async.http_proxy`。
- 确认用户环境变量 `HTTP_PROXY`、`HTTPS_PROXY` 指向 `http://127.0.0.1:7890`，而该代理当前无法完成米哈游扫码接口的 TLS 连接。
- 使用 `httpx.AsyncClient(trust_env=False)` 绕过环境代理实测成功：接口返回 HTTP 200、`retcode=0`，二维码参数为 `app_id=4`、`biz_key=hk4e_cn`。
- 为扫码登录完整链路的四个请求统一禁用环境代理：获取二维码、查询二维码状态、GameToken 换取 SToken、GameToken 换取 CookieToken。
- 未自动启动、停止或重启 AstrBot 与 `Py/main.py`；当前 AstrBot 需由用户手动重载插件或重启后才会载入代码修改。

## 删除全局代理配置

- 按用户要求删除用户级 `HTTP_PROXY`、`HTTPS_PROXY`、`ALL_PROXY` 及其小写同名环境变量。
- 关闭 Windows Internet 代理并清空 `ProxyServer=127.0.0.1:7890`；WinHTTP 保持直连。
- 广播 Windows 环境变更，使之后新启动的应用继承无代理环境。
- 未停止或重启 AstrBot 与 `Py/main.py`；已经运行的进程仍需由用户手动重启才能清除启动时继承的环境变量。

## 米游社插件全量重部署与 AstrBot 重新适配

- 删除旧的 `nonebot-plugin-mystool` 源码副本、`data/plugins/jiang_mystool` 适配层和空的插件数据目录。
- 从官方 `stable` 分支全新部署 v2.10.2，固定提交 `ac2e1242764282079e2d393c1dbc3168c9a40f34`。
- 使用 `pydantic.v1` 兼容层适配 AstrBot Python 3.13 环境中的 Pydantic 2，不降级 AstrBot 依赖。
- 新建 `jiang_mystool` v2.0.0 适配层，重新实现命令路由、二维码发送、后台扫码轮询、Token 交换、账户持久化、签到、米游币任务、地址、商品、兑换计划、便签、设置和 Cookies 导出。
- 登录轮询改为独立后台任务：命令立即返回“等待确认”，状态变化记录 `Init/Scanned/Confirmed`，成功、过期、超时和异常均通过 OneBot 主动反馈，不再依赖长时间占用 AstrBot 事件生成器。
- 修复旧适配层的米游币任务错误：改用原版 `BaseMission.available_games`，并向 `mission.sign(user)` 传递必需的用户对象。
- 全新数据文件版本为 v2.10.2，账户列表为空，扫码应用为 `app_id=4`。
- 验证通过：Python 语法、独立导入、Pydantic 兼容、六种游戏签到类/七种任务分区注册、真实二维码获取、真实状态查询 `Init`、模拟 `Confirmed` 后成功消息、任务调用签名和命令前缀路由。
- 清理官方临时克隆、旧适配参考文件及所有测试生成的 `__pycache__`。
- 按约束没有停止、启动或重启 AstrBot 与 `Py/main.py`；需用户手动重启 AstrBot 后加载全新插件。

## 扫码状态可观测性与平台二维码调整

- 用户在全新适配层中确认手机登录后仍未收到成功提示；确认新 AstrBot 进程已加载重部署版本、全局代理为空，但账户数据仍未落盘，说明未进入 Token 交换阶段。
- 将二维码应用从游戏专用 `app_id=4` 调整为通用平台应用 `app_id=5`，避免绑定流程依赖原神官服角色。
- 新增 `\\登录状态` 命令，并将非敏感状态写入 `data/nonebot-plugin-mystool/login_state.json`，便于直接定位 `QRCodeSent/Init/Scanned/Confirmed/Completed/Timeout/Failed`。
- 状态变化会主动发送微信提示：十秒未识别扫码、已扫码等待确认、手机确认成功开始换取凭据、查询错误继续重试、过期、超时和最终结果。
- 验证通过：`app_id=5` 真实二维码获取及 `Init` 查询；模拟 `Scanned → Confirmed → Completed` 时三类主动消息和状态持久化均正常。
- 未操作 AstrBot 与 `Py/main.py` 进程；代码需用户手动重启 AstrBot 后生效。

## 登录二维码应用标识最终修正

- 用户反馈 `app_id=5` 生成的二维码在手机端提示“米游姬暂时失联 / 似乎与网络失去了连接”，确认平台应用二维码不可用。
- 核对官方插件和接口应用 ID 对照后确认：官方 v2.10.2 默认值 `2` 对应《未定事件簿》，会复现用户最早遇到的未定事件簿扫码 JSON 错误；`4` 对应《原神》，且用户已确认该类型可正常扫码和手机确认。
- 最终将源码默认值和运行时 `configV2.json` 均设为 `app_id=4`，保留新版后台扫码轮询、主动状态反馈和 `\\登录状态` 命令，以继续定位确认后的 Token 交换环节。
- 使用直连请求验证 `app_id=4` 的二维码获取与初始状态查询均正常。
- 未操作 AstrBot 与 `Py/main.py` 进程。

## 米游社通行证新二维码登录

- 用户实测确认：旧游戏二维码接口中 `app_id=4` 始终停留在未扫码，`app_id=2` 虽能检测扫码但手机端确认时报 JSON 错误，说明继续切换游戏应用 ID 无法解决登录问题。
- 将 `jiang_mystool` 升级到 v2.1.0，登录链路由旧的 `hk4e-sdk` GameToken 二维码切换为米游社通行证 `app/createQRLogin` 与 `app/queryQRLoginStatus`。
- 新接口使用 `x-rpc-app_id=ddxf5dufpuyo`、`x-rpc-client_type=3`，二维码返回 `token_types=1`；确认后直接提取 SToken、mid 和米游社账号 ID，再换取 LToken 与 CookieToken 并持久化账户。
- 删除适配层中已不再使用的旧游戏二维码获取、查询和 GameToken 交换路径；保留扫码、确认、取消、过期、异常和完成状态反馈及 `\\登录状态`。
- 真实接口验证通过：二维码创建 HTTP 200，地址为 `user.mihoyo.com/login-platform/mobile.html`，初始状态查询为 `Created`（插件映射为 `Init`）。
- 模拟完整状态验证通过：`Scanned → Confirmed → SToken 提取 → 绑定成功`，主动反馈和完成状态均正常。
- 未操作 AstrBot 与 `Py/main.py` 进程。

## 米游币任务连接超时修复

- 用户登录成功后执行 `\\任务`，日志显示 `myb_missions_api.py` 的讨论区签到请求抛出 `httpx.ConnectTimeout`；账户绑定与凭据交换均已成功，失败发生在旧任务 HTTP 客户端。
- 将米游币任务链路的签到、获取帖子、阅读、点赞、分享、任务列表和任务状态共 7 处 `httpx.AsyncClient` 统一设置为 `trust_env=False`，与已验证可用的登录链路保持一致。
- 修复讨论区签到错误覆盖默认异常重试条件的问题，使连接异常经过重试后转换为 `MissionStatus(network_error=True)`，不再直接逃逸到 AstrBot 命令路由。
- 修复原版任务模块在请求尚未取得响应时仍访问 `res.text` 的二次异常，避免原始网络错误被 `UnboundLocalError` 覆盖。
- 为 AstrBot 适配层增加账户级和子任务级异常隔离；单个请求或子任务失败时返回具体失败项，避免整个 `\\任务` 只回复空白的“命令执行失败”。
- 使用刚绑定的账户执行只读验证：任务列表与任务状态均返回 HTTP 200，登录凭据有效，状态解析成功；未主动执行签到、阅读、点赞或分享动作。
- 连接失败回归验证通过：模拟 `ConnectTimeout` 会转换为 `network_error`，适配层也能捕获并返回具体失败项。
- `jiang_mystool` 版本升级至 v2.1.1；未操作 AstrBot 与 `Py/main.py` 进程。

## 米游币签到 SToken 作用域修复

- 直连修复后，讨论区签到稳定返回 `retcode=-100 / 登录失效`，但同一账户的任务状态、CookieToken 刷新和 LToken 刷新均成功，确认不是账户整体登录过期。
- 逐项验证排除了旧域名、全量 Cookie、设备 ID、App 版本、DS 和缺少请求头等单独因素；当前二维码以绝区零工具应用 `ddxf5dufpuyo`、客户端类型 3 签发的 SToken 不适用于米游社讨论区签到。
- 将通行证二维码签发身份改为米游社 `x-rpc-app_id=bll8iq97cem8`、Android 客户端类型 2；真实创建和初始查询验证成功，二维码仍返回 SToken 类型 `token_types=1`。
- 重新登录覆盖已有账户的 iOS/Android 设备 ID，统一使用二维码签发时的设备身份，避免 SToken 与后续任务请求设备不一致。
- 更新米游社 App 版本至 `2.109.0`，签到域名改为 `bbs-api.miyoushe.com`，补齐 `verify_key/csm_source/device_fp` 等现行请求头，并将讨论区操作 Cookie 收敛为 `stuid + stoken + mid`。
- `jiang_mystool` 升级至 v2.2.0；本修复需要用户重新扫码取得新作用域 SToken 后验证签到。
- 未操作 AstrBot 与 `Py/main.py` 进程。

## 多用户扫码隔离与群聊隐私保护

- 核对多用户并发模型：登录后台任务、二维码 ticket、设备 ID、状态和账户数据均按微信 `sender_id` 隔离；账户持久化写入由 `_DATA_LOCK` 串行保护，同一用户重新登录只取消自己的上一项任务。
- 修复群聊二维码泄露风险：在群聊触发 `\\登录` 时，二维码强制通过 OneBot 私发给命令发起者，群内只回复“已私发”；扫码、确认、成功、失败等后续状态也只私发本人。
- 私发失败时不再把二维码回退到群聊，改为提示用户私聊机器人重新发送 `\\登录`；私聊场景仍保留当前事件内图片回退。
- `jiang_mystool` 升级至 v2.3.0；未操作 AstrBot 与 `Py/main.py` 进程。

## 米游币完整任务与结果展示恢复

- 用户反馈 `\\任务` 只显示“综合：签到成功”，没有执行阅读、点赞、分享，结果排版也退化为两行纯文本。
- 实测确认米游社旧 `getMissions` 接口当前只返回 `continuous_sign`，旧适配层按接口返回键遍历，导致另外三项任务永远不会进入执行分支。
- 将 AstrBot 适配层改为固定兼容四项日常任务：讨论区签到、阅读 3 篇、点赞 5 次、分享 1 次；接口未返回的任务不再被误判为不存在。
- 帖子列表更新为当前 `getForumPostList` 接口，点赞更新为 `upvotePost`，综合区论坛 ID 修正为 34；真实只读请求返回 19 个可用帖子。
- 点赞任务完成后按当前维护工具的做法自动撤销点赞，避免机器人长期改变用户的帖子点赞状态。
- 新增按日期、微信发送者、米游社账户、任务分区四级隔离的本地完成缓存，以及按微信用户隔离的执行锁；同一用户同日重复执行不会重复操作，不同用户互不影响。
- 恢复完整分行结果：账户、分区、签到、阅读、点赞、分享、本次获得和当前余额均单独展示，并区分全部完成与部分失败。
- 回归验证通过：Python 语法、真实任务状态只返回签到的兼容场景、首次四任务执行、同用户同日去重、两用户隔离和当前帖子列表只读请求。
- `jiang_mystool` 升级至 v2.4.0；未启动、停止或重启 AstrBot 与 `Py/main.py`。

## 米游币点赞与分享 DS 校验修复

- 用户实测完整任务后，签到和阅读成功，点赞与分享均返回“请求校验失败”，余额仍为 30 米游币。
- 对照当前维护的 MihoyoBBSTools 提交 `f062d1fda8fab88fd312a5ca3a89537f6351943b`，确认米游社 Android 2.109.0 的 App DS salt 已更新为 `47f15f1b66bee46b816115d8e8e6ebb6`。
- 原插件点赞、分享仍调用早期 `SALT_ANDROID`（`BIPaoo...`）生成 DS，导致这两个严格校验签名的接口返回 invalid DS；阅读接口未严格拒绝旧签名，因此表现为只有阅读成功。
- 为点赞、取消点赞和分享请求统一改用当前 App DS salt，每次取消点赞前重新生成签名；不影响签到使用的带请求体 DS2，也不改动其他游戏接口签名。
- 回归验证通过：Python 语法、点赞请求签名、取消点赞请求签名、分享请求签名以及当前 salt 的 MD5 计算公式。
- `jiang_mystool` 升级至 v2.4.1；失败的点赞和分享此前未写入每日完成缓存，重新执行 `\\任务` 会只重试未完成项目。
- 未启动、停止或重启 AstrBot 与 `Py/main.py`。

## 米游币自动重试、登录后执行与每日私聊调度

- 用户在 16:29 执行任务时，任务状态接口连续超时并返回网络连接失败；16:31 再次手动执行即成功，确认属于米游社接口瞬时网络故障，不是任务或登录状态再次失效。
- 将任务网络控制统一到 AstrBot 适配层：任务列表、任务状态、签到、阅读、点赞、分享和执行后余额刷新在网络失败时均最多尝试 3 次（含首次），间隔 2 秒；底层调用使用单次请求，避免嵌套重试放大等待时间。
- 新增结构化 `MissionRunResult`，明确记录展示内容、是否全部完成和网络是否耗尽重试，供手动命令和后台自动任务共用。
- 重新设计任务结果排版为简洁卡片：标题显示全部完成或部分未完成，账户、分区、四项任务、本次获得和当前余额分组展示，去除此前过多图标与层级缩进。
- 登录成功并完成账户持久化后，自动登记用户私聊 UMO，并立即后台执行一次米游币任务；UMO 从登录事件平台前缀和 `sender_id` 生成，例如 `aiocqhttp:FriendMessage:fengchenhao002`。
- 新增持久化调度文件 `data/nonebot-plugin-mystool/mission_schedule.json`；插件加载时自动迁移现有已绑定用户，默认使用 `aiocqhttp:FriendMessage:<sender_id>`，多用户记录相互隔离。
- 新增每日 Asia/Shanghai 00:01 调度。调度器每 30 秒检查一次，并依据 `last_daily_date` 在 AstrBot 错过 00:01 或稍后重启时补跑当天任务。
- 登录后任务与每日任务若未全部完成，会私聊结果并每隔 2 分钟自动补跑，最多 5 次（首次计入）；第 5 次仍失败时明确提示本轮结束并让用户私聊发送 `\\任务` 手动执行。
- 同一用户的手动、登录后和每日任务共用执行锁，避免并发重复操作；不同用户可独立运行。
- 回归验证通过：Python 语法、真实任务状态只读请求、网络前两次失败第三次成功、连续三次网络失败、完整卡片排版、私聊 UMO 生成、登录调度登记、自动补跑成功、连续 5 次失败提示、插件初始化迁移及终止清理。
- `jiang_mystool` 升级至 v2.5.0；未启动、停止或重启 AstrBot 与 `Py/main.py`。

## 米游币任务状态符号统一

- 按用户要求将任务成功标记统一为 `✅`，失败标记统一为 `❌`，任务未全部完成及自动补跑提示统一为 `⚠️`。
- 全部完成标题调整为 `【米游币任务｜✅ 全部完成】`，部分完成标题调整为 `【米游币任务｜⚠️ 部分未完成】`。
- `jiang_mystool` 升级至 v2.5.1；未操作 AstrBot 与 `Py/main.py` 进程。

## 登录二维码冗余消息精简

- 按用户要求删除私聊扫码流程中的重复提示“二维码已发送，插件正在等待手机确认结果”。
- 删除二维码发出 10 秒后主动发送的“尚未检测到扫码，请确认扫描的是最新一张二维码”。
- 保留二维码前的扫码说明、二维码图片、检测到扫码、手机确认、最终登录结果及群聊触发时必要的“二维码已私发”提示。
- `jiang_mystool` 升级至 v2.5.2；未操作 AstrBot 与 `Py/main.py` 进程。

## 正斜杠命令与 AstrBot 唤醒词适配

- 用户要求所有米游社命令使用符合习惯的 `/命令`，不再要求输入反斜杠命令。
- 定位到 AstrBot `WakingCheckStage` 会识别 `/` 唤醒词并从 `event.message_str` 中裁掉前缀，例如 `/登录` 到插件处理阶段已变为 `登录`，导致旧自定义路由无法识别。
- 新增事件级命令解析：优先读取未被唤醒阶段修改的 `event.message_obj.message_str`，其次读取原始消息概要，最后才读取当前文本；因此 `/登录`、`/任务` 等可以在 AstrBot 使用 `/` 唤醒词时正常进入插件。
- 解析仍要求原始消息以 `/` 或旧反斜杠开头，不会把私聊中的普通“登录”“任务”等词误判为命令；旧反斜杠仅作为兼容保留。
- 将帮助、错误提示、用法说明、自动任务提示中的命令示例全部改为 `/命令`。
- 回归验证通过：AstrBot 已裁掉 `/` 的事件仍能识别、带参数命令正常解析、私聊裸文本不触发、旧反斜杠兼容及 Python 语法检查。
- `jiang_mystool` 升级至 v2.6.0；未操作 AstrBot 与 `Py/main.py` 进程。
## Music Source Switched To Xingzhige Kuwo/Bodian

- Added `XingzhigeKuwoMusic` platform backed by `https://api.xingzhige.com/API/Kuwo_BD_new/`.
- `fetch_songs()` searches by `name`; `fetch_extra()` resolves `n` into playable `raw.audioHttpsUrl` / `src`, cover art, Kuwo detail URL, quality, format, and duration.
- Added WeChat music-card support for the new platform, using the resolved audio URL as `<dataurl>` and the returned cover as `<songalbumurl>` / `<thumburl>`.
- Changed `data/config/astrbot_plugin_music_config.json` default source from `nj点歌` to `星之阁点歌`; explicit `网易点歌` / `nj点歌` commands remain available.
- Updated the plugin config schema so `星之阁点歌` and `波点点歌` appear as selectable default sources.
- Verified `我的楼兰` resolves to an HTTPS MP3 URL with cover, and `心之火` resolves to an HTTPS OGG URL with cover.
- Did not start, stop, reload, or restart AstrBot or `Py/main.py`.

## Verification

- Passed `astrbot_venv\Scripts\python.exe -m py_compile data\plugins\astrbot_plugin_music\core\platform\xingzhige.py data\plugins\astrbot_plugin_music\core\platform\__init__.py data\plugins\astrbot_plugin_music\core\sender.py data\plugins\astrbot_plugin_music\main.py`.
