# Daily Report - 2026-06-30

## 12123 挪车通知转发插件

- 新增 AstrBot 插件 `data/plugins/jiang_12123_notify/`。
- 插件启动独立 `aiohttp` webhook，默认监听 `0.0.0.0:8789/12123`。
- 支持通过 `X-Webhook-Token` header、`Authorization: Bearer` 或 URL `token` 参数认证。
- 支持识别安卓通知转发 payload 中的 `app`、`packageName`、`title`、`text`、`extras` 字段。
- 默认匹配交管 12123 包名/应用名以及“挪车 / 移车 / 一键挪车”等关键词。
- 默认推送到 AstrBot 管理员私聊：`aiocqhttp:FriendMessage:fengchenhao002`。
- 加入 5 分钟去重，避免安卓通知重复触发时刷屏。
- 新增管理员命令：
  - `\12123状态`
  - `\12123密钥`
  - `\12123测试`
  - `\12123重载`
- 新增安卓 Tasker/AutoNotification 配置说明：`data/plugins/jiang_12123_notify/ANDROID_SETUP.md`。
- 首次测试生成运行配置：`data/jiang_12123_notify/config.json`。

## Verification

- 通过 `astrbot_venv\Scripts\python.exe -m py_compile data\plugins\jiang_12123_notify\main.py`。
- 通过函数级测试：配置加载、12123 关键词匹配、非目标通知忽略、管理员 session 解析、消息格式化。
- 通过 HTTP 级测试：本地启动 webhook，POST 测试通知，mock AstrBot context 成功收到 `aiocqhttp:FriendMessage:fengchenhao002` 私聊消息。

## Public Webhook Tunnel

- 新增 SSH 反向隧道启动脚本：`scripts/start_12123_reverse_tunnel.ps1`。
- 新增开机自启计划任务注册脚本：`scripts/register_12123_tunnel_task.ps1`。
- 新增服务器 Nginx 反代模板：`deploy/nginx/12123_reverse_tunnel.conf`。
- 新增公网 webhook 部署说明：`docs/12123-public-webhook.md`。
- 验证 Windows OpenSSH client 可用，脚本 PowerShell 语法解析通过。
- 已在服务器 `47.242.208.64` 的 Nginx 现有站点中加入 `/12123` 和 `/health` 反代到 `127.0.0.1:18789`。
- 已注册并启动 Windows 计划任务 `Jiang12123ReverseTunnel`，通过 SSH 反向隧道把服务器 `127.0.0.1:18789` 转回本机 `127.0.0.1:8789`。
- 已验证 `https://xiuxianjyj.xin/health` 可访问插件，`https://xiuxianjyj.xin/12123` POST 返回 `{"ok": true, "sent": 1}`。
- 注意：当前证书只覆盖 `xiuxianjyj.xin`，不覆盖 `www.xiuxianjyj.xin`，MacroDroid 应使用根域名。

## 2026-07-01 Follow-up

- 发现 `Jiang12123ReverseTunnel` 计划任务停在 `Ready` 时公网入口会返回 Nginx `502 Bad Gateway`。
- 手动启动计划任务后，`https://xiuxianjyj.xin/health` 恢复正常，公网 POST 返回 `{"ok": true, "sent": 1}`。
- 新增守护脚本 `scripts/ensure_12123_tunnel_task.ps1` 和注册脚本 `scripts/register_12123_tunnel_watchdog.ps1`，用于定时拉起隧道任务。
- 后续发现每分钟 watchdog 直接运行 PowerShell 会弹控制台窗口，已改为 `wscript.exe` 隐藏运行，并将检查频率降为 5 分钟。

## WeChat 表情包发送修复

- 确认 `all.md` 支持 CDN 表情包发送接口 `type=11254`，请求可使用本地文件 `path`。
- 修复 `Py/main.py` 的 AstrBot -> WeChat 桥接：图片消息段现在兼容读取 `sub_type` / `subType` / `subtype`。
- 将 `emoji`、`sticker`、`mface`、`face`、`1` 识别为表情包子类型，命中后走 `helper_send_emoji`，普通图片仍走 `helper_send_image`。
- `helper_send_emoji` 的 payload 同时携带 `path` 和 `file`，兼容 `all.md` 示例和现有内部字段。

## Verification

- 通过 `C:\Users\Administrator\AppData\Local\Programs\Python\Python311-32\python.exe -m py_compile Py\main.py`。

## 点歌网易云卡片发送修复

- 对比抓到的第三方音乐卡片 `11061` raw XML，确认可播放卡片核心是 `<appmsg><type>3</type><dataurl>...</dataurl><songalbumurl>...</songalbumurl>`。
- `astrbot_plugin_music` 的网易云卡片模式恢复为优先构造微信音乐 appmsg XML 并调用 `send_raw_xml`，仅在 raw XML API 明确失败时才降级为 `wechat_link_card`。
- `Py/main.py` 的 `send_raw_xml` 支持 `send_type` 参数与 `WECHAT_RAW_XML_SEND_TYPE` 环境变量，默认仍用 `11214`，方便后续实机尝试不同 DLL 发送类型。

## Verification

- 通过 `astrbot_venv\Scripts\python.exe -m py_compile data\plugins\astrbot_plugin_music\core\sender.py`。
- 通过 `C:\Users\Administrator\AppData\Local\Programs\Python\Python311-32\python.exe -m py_compile Py\main.py`。

## 图来图片撤回修复

- 修复 `图来` 定时撤回失效：撤回请求现在显式传 `msg_type="image"`，只撤回图片消息，避免被队列里的文字、卡片等其他 bot 消息挡住。
- `Py/main.py` 的撤回队列现在会记录发送消息类型，并支持按类型查找待撤回消息。
- 发送回包 `11047` 的 trace 提取增加兜底，从顶层消息和 data 中都尝试读取，减少图片发送后未入撤回队列的情况。

## Verification

- 通过 `C:\Users\Administrator\AppData\Local\Programs\Python\Python311-32\python.exe -m py_compile Py\main.py`。
- 通过 `astrbot_venv\Scripts\python.exe -m py_compile data\plugins\jiang_commands\main.py`。
- 通过轻量断言确认 `图来` 撤回请求带 `msg_type="image"`，桥接层会按图片类型过滤撤回队列。

## 点歌网易云源稳定性

- 将默认点歌源从网易云 NodeJS 镜像切回网易云 Web 搜索，避免 `https://163api.qijieya.cn` 返回 524 导致 `搜索无结果`。
- 网易云卡片发送不再等待第三方 `meting/qijieya` 补全音频或封面，直接使用网易云 `song/media/outer/url` 作为 `dataurl` 兜底。
- 点歌搜索增加兜底：如果当前默认网易源无结果，会尝试切到网易云 Web 源再搜一次。

## Verification

- 通过 `astrbot_venv\Scripts\python.exe -m py_compile data\plugins\astrbot_plugin_music\main.py data\plugins\astrbot_plugin_music\core\sender.py`。
- 通过轻量验证确认配置默认源为 `网易点歌`，网易云卡片路径不再调用 `fetch_extra`。
- 通过网易云 Web 搜索验证 `月亮之上` 可返回歌曲 ID `354824`。

## QQ 音乐可播放卡片

- 根据抓到的 `11061` QQ 音乐 appmsg，确认微信音乐卡片使用 `<appmsg appid="wx5aa333606550dfd5"><type>3</type>` 结构。
- `astrbot_plugin_music` 的 QQ/TXQQ 源现在支持 `card` 模式，会构造 QQ 音乐 appmsg XML 并通过 `send_raw_xml` 发送。
- 默认点歌源已从网易云切换为 QQ 音乐：`点歌 歌名` 走 QQ，`网易点歌 歌名` 仍可走网易。
- 已关闭音乐插件发送后的热评附加消息，避免卡片后面再跟额外文本。

## Verification

- 通过 `astrbot_venv\Scripts\python.exe -m py_compile data\plugins\astrbot_plugin_music\core\sender.py data\plugins\astrbot_plugin_music\core\platform\txqq.py data\plugins\astrbot_plugin_music\main.py`。
- 通过 `C:\Users\Administrator\AppData\Local\Programs\Python\Python311-32\python.exe -m py_compile Py\main.py`。
- 通过轻量烟测确认 QQ 音乐 XML 包含 `type=3`、QQ 音乐 appid、`songmid` 与可播放 `dataurl`。

## 网易云音乐可播放卡片

- 网易云音乐与网易云 NodeJS 源也改为直接构造微信 `<appmsg type=3>` 音乐卡片，不再走 OneBot `music(type=163)` 的普通适配路径。
- 新增通用微信音乐 XML 构造逻辑，QQ 音乐和网易云音乐共用同一套 appmsg 模板。
- 网易云卡片使用 `https://music.163.com/song?id=...` 作为打开链接，优先使用解析到的音频 URL 作为 `dataurl`，没有音频 URL 时回退到网易云 outer song URL。

## Verification

- 通过 `astrbot_venv\Scripts\python.exe -m py_compile data\plugins\astrbot_plugin_music\core\sender.py data\plugins\astrbot_plugin_music\main.py`。
- 通过轻量烟测确认网易云 XML 包含 `type=3`、`网易云音乐` appname、网易云播放页与 `dataurl`。

## 点歌命令简化

- 按最终需求取消 QQ 音乐路径：音乐插件只响应 `点歌 xxx` 一个入口。
- `点歌 xxx` 固定使用网易云 NodeJS 源，并只发送网易云音乐微信 appmsg 卡片。
- `QQ点歌`、`网易点歌`、`网易nj` 等平台命令不再由音乐插件响应。
- Self Learning 的命令过滤同步收窄为只把 `点歌 xxx` 当命令，避免多平台点歌词继续特殊处理。

## Verification

- 通过 `astrbot_venv\Scripts\python.exe -m py_compile data\plugins\astrbot_plugin_music\main.py data\plugins\astrbot_plugin_music\core\sender.py data\plugins\astrbot_plugin_self_learning\services\commands\command_filter.py data\plugins\astrbot_plugin_self_learning\services\learning\sample_filter.py`。
- 通过轻量断言确认 `点歌 月亮之上` 被识别为命令，`网易点歌 月亮之上` / `QQ点歌 月亮之上` 不再被识别。
- 修复点歌入口简化后遗留的 `NameError: name 'cmd' is not defined`，搜索歌曲时不再传平台命令参数。
- 通过 32-bit Python 轻量运行时验证：`sub_type=emoji`、`subType=emoji`、`sub_type=1` 均识别为表情包，未带子类型的图片仍识别为普通图片。
- 根据运行日志补充兜底：AstrBot 实际发送的 meme_manager 图片段会丢失 `sub_type`，现在 `Py/main.py` 会将 `data/plugin_data/meme_manager/memes/` 下的图片自动识别为表情包，继续走 `type=11254`。

## Self Learning 配置

- 已开启 `data/config/astrbot_plugin_self_learning_config.json` 中的 `Runtime_Internal_Settings.enable_llm_hooks`，允许 Self Learning 在 LLM 请求前注入已学习的上下文。
- 使用 `utf-8-sig` 解析配置文件验证字段值为 `true`。

## 音乐插件微信卡片适配

- 修复 `astrbot_plugin_music` 在私聊中 `点歌` / `/点歌` 不触发的问题：插件现在直接识别点歌关键词，不再依赖 `event.is_at_or_wake_command`。
- 恢复音乐插件发送顺序为 `card -> file -> record -> text`。
- `Py/main.py` 新增 OneBot `music` 消息段到微信链接卡片的转换，网易云 `type=163` 会生成 `https://music.163.com/song?id=...` 链接。
- 微信链接卡片发送回调改为使用 `all.md` 中明确支持的 CDN 链接卡片接口 `type=11236`。

## Verification

- 通过 `C:\Users\Administrator\AppData\Local\Programs\Python\Python311-32\python.exe -m py_compile Py\main.py data\plugins\astrbot_plugin_music\main.py`。
- 通过轻量运行时验证：`{"type":"music","data":{"type":"163","id":"123456"}}` 可转换为微信卡片数据。
- 兼容群消息前置的“系统身份提示”文本：`astrbot_plugin_music` 现在会先清理该前缀，再识别 `点歌` / `/点歌` 命令，避免被 Self Learning 注入文本干扰触发。
- 修复 `图来` 失效：`jiang_commands` 不再使用严格的 `^图来$` 正则，而是先清理系统身份提示，再兼容识别 `图来` / `/图来`。

## 命令污染排查

- 已完成项目级命令触发排查，确认当前真正受“系统身份提示”前缀影响的高风险入口主要集中在“纯文本命令/手动 split 解析”这类插件。
- 已修复 `jiang_lol_match`：`游戏名#TAG` 现在会先清理系统身份提示，再匹配查询格式，群里不用前缀也能正常触发。
- 已修复 `jiang_commands` 中的 `天气` / `油价`：现在会先清理系统身份提示，并兼容 `城市天气`、`/城市天气`、`\城市天气` 与对应油价写法。
- 已修复 `astrbot_plugin_self_learning` 的命令识别与 payload 提取：`command_filter.py` 与 `services/commands/handlers.py` 均已在解析前去掉系统身份提示，避免把命令当普通聊天采集，或把参数解析歪掉。
- 已修复 `astrbot_plugin_proactive_chat` 的命令判定：主动插话逻辑在判断“这是不是命令”前会先移除系统身份提示，避免对命令消息误插话。
- 已修复 `astrbot_plugin_apis` 的文本入口：即便未来关闭 `need_prefix`，也不会再被系统身份提示顶坏首词匹配。
- 已修复 `图来` 的发图实现：不再走 `Image.fromBase64(...)`，改为先落本地临时文件再 `image_result(...)` 发送，规避大体积 Base64 图片段导致 OneBot/桥接链路重连的问题。
- 已将 `astrbot_plugin_music` 的 `select_mode` 从 `text` 调整为 `single`，`点歌 月亮之上` 这类命令不再先返回候选列表，而是直接取首条结果并发送音乐卡片。

## Verification

- 通过 `astrbot_venv\Scripts\python.exe -m py_compile data\plugins\jiang_lol_match\main.py data\plugins\jiang_commands\main.py data\plugins\astrbot_plugin_self_learning\services\commands\command_filter.py data\plugins\astrbot_plugin_self_learning\services\commands\handlers.py data\plugins\astrbot_plugin_proactive_chat\core\message_events.py data\plugins\astrbot_plugin_apis\main.py`。

## QQ 音乐卡片探测

- `Py/main.py` 现在会在收到 `11061` appmsg 时保存完整原始消息，便于分析微信音乐卡片 XML。
- 最近一次 `11061` 会落盘到 `Py/data/appmsg/last_11061_raw.xml`，完整 JSON 会落盘到 `Py/data/appmsg/last_11061.json`。
- 新增 OneBot API 动作 `get_last_appmsg_raw_xml`，可直接读取最近一次 appmsg XML。
- `send_raw_xml` 现在支持 `content="__last_appmsg__"`，方便后续原样回放 QQ 音乐卡片测试。

## Verification

- 通过 `C:\Users\Administrator\AppData\Local\Programs\Python\Python311-32\python.exe -m py_compile Py\main.py`。
