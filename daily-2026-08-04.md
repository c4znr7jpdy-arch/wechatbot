# 2026-08-04 开发日报

## 群分析报告恢复群内备注

- 定位 7 天群分析报告显示 `wxid`/微信昵称而非群内备注的原因：群分析适配器调用标准 OneBot `get_group_member_list`，而微信桥接层提供的是自定义 `get_group_member_cache`，导致昵称映射始终为空。
- 修改 `astrbot_plugin_qq_group_daily_analysis` 的 OneBot 适配器：微信 `@chatroom` 群优先读取桥接层成员缓存，并兼容直接数据与 OneBot `data` 包装两种返回格式；非微信平台继续使用标准成员列表接口。
- 成员显示名按“群内备注 `display_name` → 微信昵称 `nickname` → 通讯录备注 `remark`”回退，并通过 `sender_card` 传递给统计、话题、群友画像和金句分析。
- 使用真实群成员缓存和 7 天消息库验证：成功读取 1000 条消息、66 位发言者；33 位命中群内备注，33 位命中昵称回退，66 位全部解析，残留占位名 0 条。
- 适配器通过 AstrBot Python 3.13 `py_compile` 及两种接口返回格式的定向模拟测试。
- 未启动、停止或重启 AstrBot、`Py/main.py`，也未热重载插件；当前运行实例需手动热重载群分析插件后生效。

## AngelHeart 被 @ 回复提速

- 根据 10:16 的群聊日志拆分延迟：AngelHeart 秘书模型判断约 14 秒，LivingMemory 召回约 2.5 秒，主模型生成约 2.6 秒；主要瓶颈是明确被 @ 后仍重复调用秘书模型。
- 启用 AngelHeart 的 `force_reply_when_summoned`、`reply_even_not_questioned` 与 `fast_reply_when_summoned`，明确呼唤时直接进入主回复链，保留长期记忆召回和正式回复生成。
- 预期同类简单 @ 消息从约 20 秒降至约 5 秒；复杂回复的主模型与记忆能力不受影响。
- 配置 JSON 已通过 UTF-8 BOM 兼容解析和布尔值断言；项目环境未安装 `pytest`，因此未运行插件现有 pytest 测试。
- 已通过 AstrBot WebUI 热重载 `astrbot_plugin_angel_heart`；平台日志确认插件于 10:23:01 完成初始化。未启动、停止或重启 AstrBot、`Py/main.py`。

## 短剧搜索与微信播放卡片

- 新增 `jiang_short_drama` AstrBot 插件，严格匹配 `短剧 + ASCII 空格 + 剧名`；无空格、全角空格、命令前缀和前导空格均不会误触发，并会阻断后续 AI 对话链。
- 按用户提供的「短百影视」UserScript 接入 7 个 HTTPS 苹果 CMS/VOD JSON 源，以受控并发搜索，按标题匹配度与可用集数选择最佳 HLS 资源；单源超时、反爬或格式异常会自动跳过。
- 解析 `vod_play_url` 的多线路与多集格式，通过现有 OneBot `wechat_link_card` → 微信 11039 链路发送可点击播放卡片；卡片发送失败时降级为文本链接。
- 默认卡片直达第一集 m3u8，并提供 `player_url_template` 配置，便于后续切换到公网 HTTPS HLS 播放页面。
- 添加严格触发、标题归一化、多线路解析与最佳标题选择的离线单元测试；真实源烟测精确命中剧名，首集返回 HTTP 206 与有效 HLS 清单。未启动、停止、重启或热重载 AstrBot / `Py/main.py`。

### 微信内置浏览器拦截修复

- 根据微信 `weixin110.qq.com` 安全页与普通浏览器可正常播放的对比，确认故障是 m3u8 资源站域名作为顶层 URL 被微信风控拦截，并非资源失效。
- 卡片默认地址改为微信兼容的 HTTPS HLS 播放页 `m3u8-player.cc/player?url=...`，资源站 URL 作为编码参数交给页面播放器加载；保留自定义播放器模板和显式 `direct` 直链模式。
- 目标示例流返回 HTTP 200、`application/vnd.apple.mpegurl` 以及 `Access-Control-Allow-Origin: *`；默认播放页返回 HTTP 200，满足跨域 HLS 加载条件。
- 新增默认播放页、显式直链和自定义模板三组测试；同步更新已有运行配置。未重载 AstrBot 或 `Py/main.py`。

### 完整版优先与分集选择

- 修正原先“同分时优先集数最多”导致 69 个数分钟分集压过 2.5 小时完整版的问题；选择流程改为“精准同名候选池 → 无精准结果才启用模糊候选池 → 候选池内优先实际时长最长的完整版”。
- 新增 HLS 时长探测，既能累计媒体清单的 `EXTINF`，也能继续解析 master playlist 的子清单；单线路达到默认 30 分钟才认定为完整版，避免把只有一集的更新中资源误判成完整版。
- 新增飘零资源。真实核验《可否许我再少年》存在同名 170.2 分钟完整版，《可否许我再少年2》存在同名 167.5 分钟完整版；模糊命中的“第二季”不会混入已有精准结果。
- 支持 `短剧 剧名 第N集` 精确选集，并兼容 `第1-20集` 这类合并播放段；没有完整版时默认回退分集源，卡片提示选集格式。
- 新增精准/模糊池隔离、HLS 时长、master 变体、分集范围和目标集播放 URL 测试。未重载 AstrBot 或 `Py/main.py`。

### 空结果二次模糊检索

- 定位 `全球杀机`、`腊肉` 无模糊结果的原因：原实现只对资源站首次返回列表做相似度排序，资源站以原词返回空列表时没有任何候选可排。
- 精准同名为空时新增第二轮检索：优先使用已知近形/别名，否则对不少于 4 字的输入生成安全前后缩短词；二次结果仍按原始输入重新评分，不会绕过最低相似度。
- 标题评分新增等长窗口单字差异容错，可将 `全球杀机` 识别到资源站收录的《全球杀戮：开局觉醒SSS级天赋动态漫》；精准结果存在时仍完全隔离模糊候选。
- 对两字短词禁止拆成单字扩搜，避免 `腊肉` 误命中“老腊肉队”等无关内容；已识别可能标题《致命的腊肉》《腊肉风云》，但当前接入资源站没有 m3u8 时明确回复暂无可播放资源。
- 增加错字评分、别名扩搜、安全缩词和资源缺失提示测试。未重载 AstrBot 或 `Py/main.py`。
