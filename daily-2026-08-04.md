# 2026-08-04 开发日报

## 群分析报告恢复群内备注

- 定位 7 天群分析报告显示 `wxid`/微信昵称而非群内备注的原因：群分析适配器调用标准 OneBot `get_group_member_list`，而微信桥接层提供的是自定义 `get_group_member_cache`，导致昵称映射始终为空。
- 修改 `astrbot_plugin_qq_group_daily_analysis` 的 OneBot 适配器：微信 `@chatroom` 群优先读取桥接层成员缓存，并兼容直接数据与 OneBot `data` 包装两种返回格式；非微信平台继续使用标准成员列表接口。
- 成员显示名按“群内备注 `display_name` → 微信昵称 `nickname` → 通讯录备注 `remark`”回退，并通过 `sender_card` 传递给统计、话题、群友画像和金句分析。
- 使用真实群成员缓存和 7 天消息库验证：成功读取 1000 条消息、66 位发言者；33 位命中群内备注，33 位命中昵称回退，66 位全部解析，残留占位名 0 条。
- 适配器通过 AstrBot Python 3.13 `py_compile` 及两种接口返回格式的定向模拟测试。
- 未启动、停止或重启 AstrBot、`Py/main.py`，也未热重载插件；当前运行实例需手动热重载群分析插件后生效。

## AngelHeart 被 @ 回复提速

- 根据 10:16 的群聊日志拆分延迟：AngelHeart 秘书模型判断约 14 秒，LivingMemory 召回约 2.5 秒，主模型生成约 2.6 秒；主要瓶颈是明确被 @ 后仍重复调用秘书模型。
- 启用 AngelHeart 的 `force_reply_when_summoned`、`reply_even_not_questioned` 与 `fast_reply_when_summoned`，明确呼唤时直接进入主回复链，保留长期记忆召回和正式回复生成。
- 预期同类简单 @ 消息从约 20 秒降至约 5 秒；复杂回复的主模型与记忆能力不受影响。
- 配置 JSON 已通过 UTF-8 BOM 兼容解析和布尔值断言；项目环境未安装 `pytest`，因此未运行插件现有 pytest 测试。
- 已通过 AstrBot WebUI 热重载 `astrbot_plugin_angel_heart`；平台日志确认插件于 10:23:01 完成初始化。未启动、停止或重启 AstrBot、`Py/main.py`。

## 短剧搜索与微信播放卡片

- 新增 `jiang_short_drama` AstrBot 插件，严格匹配 `短剧 + ASCII 空格 + 剧名`；无空格、全角空格、命令前缀和前导空格均不会误触发，并会阻断后续 AI 对话链。
- 按用户提供的「短百影视」UserScript 接入 7 个 HTTPS 苹果 CMS/VOD JSON 源，以受控并发搜索，按标题匹配度与可用集数选择最佳 HLS 资源；单源超时、反爬或格式异常会自动跳过。
- 解析 `vod_play_url` 的多线路与多集格式，通过现有 OneBot `wechat_link_card` → 微信 11039 链路发送可点击播放卡片；卡片发送失败时降级为文本链接。
- 默认卡片直达第一集 m3u8，并提供 `player_url_template` 配置，便于后续切换到公网 HTTPS HLS 播放页面。
- 添加严格触发、标题归一化、多线路解析与最佳标题选择的离线单元测试；真实源烟测精确命中剧名，首集返回 HTTP 206 与有效 HLS 清单。未启动、停止、重启或热重载 AstrBot / `Py/main.py`。

### 微信内置浏览器拦截修复

- 根据微信 `weixin110.qq.com` 安全页与普通浏览器可正常播放的对比，确认故障是 m3u8 资源站域名作为顶层 URL 被微信风控拦截，并非资源失效。
- 卡片默认地址改为微信兼容的 HTTPS HLS 播放页 `m3u8-player.cc/player?url=...`，资源站 URL 作为编码参数交给页面播放器加载；保留自定义播放器模板和显式 `direct` 直链模式。
- 目标示例流返回 HTTP 200、`application/vnd.apple.mpegurl` 以及 `Access-Control-Allow-Origin: *`；默认播放页返回 HTTP 200，满足跨域 HLS 加载条件。
- 新增默认播放页、显式直链和自定义模板三组测试；同步更新已有运行配置。未重载 AstrBot 或 `Py/main.py`。

### 完整版优先与分集选择

- 修正原先“同分时优先集数最多”导致 69 个数分钟分集压过 2.5 小时完整版的问题；选择流程改为“精准同名候选池 → 无精准结果才启用模糊候选池 → 候选池内优先实际时长最长的完整版”。
- 新增 HLS 时长探测，既能累计媒体清单的 `EXTINF`，也能继续解析 master playlist 的子清单；单线路达到默认 30 分钟才认定为完整版，避免把只有一集的更新中资源误判成完整版。
- 新增飘零资源。真实核验《可否许我再少年》存在同名 170.2 分钟完整版，《可否许我再少年2》存在同名 167.5 分钟完整版；模糊命中的“第二季”不会混入已有精准结果。
- 支持 `短剧 剧名 第N集` 精确选集，并兼容 `第1-20集` 这类合并播放段；没有完整版时默认回退分集源，卡片提示选集格式。
- 新增精准/模糊池隔离、HLS 时长、master 变体、分集范围和目标集播放 URL 测试。未重载 AstrBot 或 `Py/main.py`。

### 空结果二次模糊检索

- 定位 `全球杀机`、`腊肉` 无模糊结果的原因：原实现只对资源站首次返回列表做相似度排序，资源站以原词返回空列表时没有任何候选可排。
- 精准同名为空时新增第二轮检索：优先使用已知近形/别名，否则对不少于 4 字的输入生成安全前后缩短词；二次结果仍按原始输入重新评分，不会绕过最低相似度。
- 标题评分新增等长窗口单字差异容错，可将 `全球杀机` 识别到资源站收录的《全球杀戮：开局觉醒SSS级天赋动态漫》；精准结果存在时仍完全隔离模糊候选。
- 对两字短词禁止拆成单字扩搜，避免 `腊肉` 误命中“老腊肉队”等无关内容；已识别可能标题《致命的腊肉》《腊肉风云》，但当前接入资源站没有 m3u8 时明确回复暂无可播放资源。
- 增加错字评分、别名扩搜、安全缩词和资源缺失提示测试。未重载 AstrBot 或 `Py/main.py`。

### 分集播放器与自动连播

- 新增插件内置的令牌式 HLS 分集播放器：支持下拉选集、上一集、下一集、重试、本集结束自动播放下一集，并在浏览器本地记住当前集及各集播放进度。
- `短剧 剧名 第N集` 生成的卡片会以目标集作为播放器初始集；播放列表只在机器人内存中短期保存，卡片 URL 不包含整套 m3u8 地址，默认 6 小时失效且数量受限。
- 分集播放器独立监听 `127.0.0.1:6197`，仅在配置 `episode_player_public_base_url` 后启用；未配置公网入口、监听失败、单线路或完整版结果均自动回退现有单集公共播放器。
- 增加随机令牌、过期回收、公网地址校验、目标集初始化和单线路回退测试；20 项单元测试全部通过，并完成临时本地 HTTP 烟测：播放页、剧集 API、播放器脚本均返回 HTTP 200。
- 未启动、停止、重启或热重载 AstrBot / `Py/main.py`；代码生效仍需用户授权后重载插件，并需为 `127.0.0.1:6197/short-drama/` 配置可供微信访问的公网 HTTPS 反向代理。

### 分集播放器公网部署

- 用户新增 `player.xiuxianjyj.xin` A 记录指向 VPS `47.242.208.64`；权威 DNS 与公共递归 DNS 均确认解析生效。
- 在 VPS 新增独立 Nginx 站点和 Let’s Encrypt 证书，证书有效期至 2026-11-02，Certbot 已配置自动续期；没有修改 `xiuxianjyj.xin` 的 NewAPI 根站点配置，部署后根站仍返回 HTTP 200。
- 播放器公网入口为 `https://player.xiuxianjyj.xin/short-drama/`，HTTP 自动 301 到 HTTPS，公网健康接口返回 `jiang_short_drama` JSON 200。
- SSH 反向隧道改用 VPS 本地 Unix Socket `/run/short-drama-player.sock` 连接 Windows `127.0.0.1:6197`，权限为 `660 root:www-data`；移除早期 TCP 16197 监听，服务器不额外暴露播放器端口。
- 新增 Windows 隐藏隧道脚本、断线自动重连、当前用户登录启动项及可选的管理员计划任务注册脚本；当前会话因无管理员权限未注册计划任务，改用无需提权的登录启动项。
- 经用户授权通过 AstrBot WebUI 热重载 `jiang_short_drama`，面板确认版本 1.2.0、状态启用且“重载成功”；未启动、停止或重启 AstrBot、`Py/main.py`。

### 分集播放器移动端界面重构

- 根据 Android 微信内置浏览器实机截图重构播放器页面：移除占据页面的大型“上一集/下一集/重试”按钮，将上下集改为视频画面左右两侧的半透明小图标，标题和自动连播区域同步压缩。
- 移除会触发 Android 全屏且难以取消的原生 `<select>`，改为带遮罩、明确关闭按钮、当前集高亮和四列剧集网格的自定义底部面板；面板最高占视口 68%，支持点击遮罩、关闭按钮及 Escape 退出。
- 新增真实 HLS 清晰度菜单：master playlist 提供多码率时显示自动、1080P、720P、480P 等并记忆用户选择；单码率媒体清单只显示“原画”，不伪造不存在的清晰度。
- 播放器监听视频 `videoWidth/videoHeight`，按实际宽高比动态计算容器尺寸；横屏视频使用可用宽度，竖屏视频居中并将高度限制在视口约 72%，保持 `object-fit: contain` 不裁切变形。
- 使用 390×844 手机视口完成视觉检查：59 集选集面板可关闭且当前集高亮；多码率测试流的双列清晰度菜单完整展示并成功切换、记忆 720P；单码率实剧线路显示“原画”。
- 播放器脚本增加 `v=1.2.1` 缓存版本并缩短缓存时间；22 项单元测试及前端 JavaScript 语法检查通过。通过 AstrBot WebUI 热重载至 1.2.1，未启动、停止或重启 AstrBot、`Py/main.py`。
- 修复 SSH 反向隧道异常退出后遗留 `/run/short-drama-player.sock`、导致后续重连失败和公网 502 的问题：每次连接前安全清理指定套接字，并在远端会话退出时自动清理。主动终止隧道进程演练后约 14 秒自动建立新连接，套接字恢复为 `660 root:www-data`，公网健康接口再次返回成功。

### 分集播放器全屏控件修复

- 定位全屏状态缺少上下集按钮的原因：浏览器原生全屏只提升 `<video>` 元素，播放器容器中的自定义叠层不会随之进入全屏。
- 使用 `controlslist="nofullscreen"` 移除原生视频全屏入口，新增播放器容器全屏按钮；标准 Fullscreen API 不可用或被拒绝时回退为页面级全屏，确保上下集、当前集、清晰度和退出全屏控件始终属于同一叠层。
- 在实际浏览器全屏中验证：第 1 集显示下一集，第 2 集同时显示上一集与下一集，点击后集数和视频线路正确切换；23 项单元测试、JavaScript 语法及差异检查通过。通过 AstrBot WebUI 热重载至 1.2.2，公网脚本与健康接口验证成功，未启动、停止或重启 AstrBot、`Py/main.py`。

### 全屏选集面板修复

- 定位全屏点击左上角选集后看不到列表的原因：选集面板原先位于全屏播放器容器之外，因此被浏览器全屏顶层隔离。
- 将同一套可关闭选集面板移入播放器容器的 DOM 子树，普通页面和全屏状态继续共用，无须退出或切回竖屏。
- 使用 59 集测试数据完成实际全屏验证：横屏面板可见、当前集高亮、列表可滚动；选择第 10 集后面板关闭、线路切换且播放器保持全屏。23 项单元测试、JavaScript 语法和差异检查通过，并通过 AstrBot WebUI 热重载至 1.2.3；未启动、停止或重启 AstrBot、`Py/main.py`。

### 爱奇艺式竖屏选集排版

- 参考用户提供的爱奇艺播放器截图，只提取简洁的选集信息架构，不加入讨论、角色、推荐等额外模块。
- 竖屏页面在播放器下方使用固定五列网格完整铺开全部剧集，标准集名缩短为纯数字，当前集高亮；列表随页面纵向滚动，不使用横向滑动或二次展开。
- 播放器左上角入口固定显示“选集”，当前集数移到画面顶部正中；普通页面点击“选集”滚动到下方列表，全屏点击仍打开播放器内部选集层。
- 使用 390×844 手机视口和 59 集数据完成视觉与交互验证：实际内容宽 375px、五列、59 个按钮、页面无横向溢出，当前集中心偏差为 0；选择第 12 集后顶部文字与高亮同步更新，并完成全屏选集回归验证。23 项单元测试、JavaScript 语法和差异检查通过，通过 AstrBot WebUI 热重载至 1.2.4；未启动、停止或重启 AstrBot、`Py/main.py`。
