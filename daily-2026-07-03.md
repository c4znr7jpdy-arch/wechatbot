# Daily Report - 2026-07-03

## Weather Card Alert Text

- Fixed the disaster alert chip so long warning text can wrap to two lines instead of being forced into a single early ellipsis.
- Kept wrapped continuation text aligned with the alert title text, not with the colored severity dot.
- Added a two-line clamp so extremely long warnings still stay inside the glass chip.
- Simplified alert text extraction to show only the short warning name, such as `高温黄色预警`, without the following signal details or forecast description.

## Verification

- Ran `astrbot_venv\Scripts\python.exe -m py_compile ai_plugin\ai_plugin\weather_image.py`.
- Rendered a local weather card preview with a long yellow high-temperature alert and confirmed the warning text wraps cleanly without overlapping nearby UI.
- Checked long alert title samples and confirmed they reduce to short labels like `高温黄色预警` and `雷电黄色预警`.

## 图来 Auto Recall

- Changed `jiang_commands` 图来 sending to prefer a direct OneBot `send_group_msg` / `send_private_msg` call with `auto_recall_delay=20`, so the WeChat layer can bind the sent image trace before scheduling recall.
- Kept the old `image_result` path as a fallback if direct OneBot sending is unavailable.
- Updated `Py/main.py` to read `auto_recall_delay` from `send_message`, `send_group_msg`, and `send_private_msg` requests and pass it into `helper_send_image`.

## Verification

- Ran `C:\Users\Administrator\AppData\Local\Programs\Python\Python311-32\python.exe -m py_compile Py\main.py`.
- Ran `astrbot_venv\Scripts\python.exe -m py_compile data\plugins\jiang_commands\main.py`.
