# Daily Report - 2026-07-03

## Weather Card Alert Text

- Fixed the disaster alert chip so long warning text can wrap to two lines instead of being forced into a single early ellipsis.
- Kept wrapped continuation text aligned with the alert title text, not with the colored severity dot.
- Added a two-line clamp so extremely long warnings still stay inside the glass chip.
- Simplified alert text extraction to show only the short warning name, such as `高温黄色预警`, without the following signal details or forecast description.

## Verification

- Ran `astrbot_venv\Scripts\python.exe -m py_compile ai_plugin\ai_plugin\weather_image.py`.
- Rendered a local weather card preview with a long yellow high-temperature alert and confirmed the warning text wraps cleanly without overlapping nearby UI.
- Checked long alert title samples and confirmed they reduce to short labels like `高温黄色预警` and `雷电黄色预警`.

## 图来 Auto Recall

- Changed `jiang_commands` 图来 sending to prefer a direct OneBot `send_group_msg` / `send_private_msg` call with `auto_recall_delay=20`, so the WeChat layer can bind the sent image trace before scheduling recall.
- Kept the old `image_result` path as a fallback if direct OneBot sending is unavailable.
- Updated `Py/main.py` to read `auto_recall_delay` from `send_message`, `send_group_msg`, and `send_private_msg` requests and pass it into `helper_send_image`.

## Verification

- Ran `C:\Users\Administrator\AppData\Local\Programs\Python\Python311-32\python.exe -m py_compile Py\main.py`.
- Ran `astrbot_venv\Scripts\python.exe -m py_compile data\plugins\jiang_commands\main.py`.

## Weather Card Location Icon

- Reworked the header location marker from a capsule-style CSS shape into a round map-pin shape.
- Kept the existing light-blue glass palette while adding a clearer center dot, highlight, and softer shadow.
- Replaced the forecast footer wind-speed display with per-day sunrise times from `suntimes.sunrise`.
- Changed the footer icon from wind streaks to a CSS sunrise marker with a 3D sun and two layered horizon lines.
- Added sunrise values and the current date to the weather-card cache key so daily sunrise changes are reflected.
- Enabled apihz `suntimetype=1`; `suntimetype=0` was preventing sunrise data from being returned.
- Fixed sunrise parsing to read the root-level 7-day `suntimes` array documented by apihz.

## Verification

- Rendered a local weather card preview with Playwright and confirmed the location marker is recognizable and does not collide with the subtitle text.
- Ran `astrbot_venv\Scripts\python.exe -m py_compile ai_plugin\ai_plugin\weather_image.py`.
- Built weather-card HTML with synthetic `suntimes.sunrise` values and confirmed it renders per-day sunrise times without reusing `wind_speed`.
- Built weather-card HTML with root-level `suntimes` array data and confirmed all 7 sunrise values render.
- Rendered a local sunrise-card preview and confirmed the sunrise icon and time fit inside each forecast column.

## Group Leave Card Identity

- Updated the WeChat bridge group-decrease notice to preserve `wx_display_name`, `wx_nickname`, and `wx_avatar` before removing the departing member from the local group-member cache.
- Updated the group notice card plugin to show the member's group display name instead of the raw WxID, while also showing the member's own nickname.
- Corrected field semantics so `display_name` remains the group nickname only and no longer falls back to `nickname`; the card labels `nickname` as the member's name.
- Made group notice avatar fallback parsing accept both direct `avatar` responses and wrapped `{data: {avatar: ...}}` action responses.

## Verification

- Ran `C:\Users\Administrator\AppData\Local\Programs\Python\Python311-32\python.exe -m py_compile Py\main.py`.
- Ran `astrbot_venv\Scripts\python.exe -m py_compile data\plugins\jiang_group_notice\main.py`.

## Group Notice WebUI Config

- Added a WebUI plugin configuration schema for `jiang_group_notice`.
- Added configurable welcome-card title and final welcome line, with placeholder support for `{group_name}`, `{display_name}`, `{nickname}`, and `{user_id}`.
- Changed the welcome card renderer to read config values at send time so saved WebUI changes apply to the next card without hard-coded text changes.

## Verification

- Ran `astrbot_venv\Scripts\python.exe -m py_compile data\plugins\jiang_group_notice\main.py`.
- Validated `data\plugins\jiang_group_notice\_conf_schema.json` with Python `json.load`.

## Group Member Refresh Timing

- Changed group-increase cards to request `11032` with a trace, wait for the response, then send the card from the refreshed member cache.
- Suppressed the welcome card if `11032` does not return or the new member is not present in the refreshed cache, avoiding fallback cards with mismatched identity data.
- Refresh `11032` on existing-member `11098` update events and on `11200` member profile/display-name notifications to overwrite stale cached group nicknames.
- Kept leave-card identity cache-first, then refresh `11032` after the leave notice so the remaining group cache is current.
- Added a `group_member_update` notice and card for `11200` nickname/profile changes when the event includes enough data to identify both the room and member.
- Added support for `11200` payloads where the changed member arrives as `member_list[0].wxid`, including non-`wxid_` IDs such as the admin account.
- Removed the redundant group-name line from the nickname-change card because the card is already sent in the affected group.

## Verification

- Ran `C:\Users\Administrator\AppData\Local\Programs\Python\Python311-32\python.exe -m py_compile Py\main.py`.
- Ran `astrbot_venv\Scripts\python.exe -m py_compile data\plugins\jiang_group_notice\main.py`.
