# Daily Report 2026-06-29

## Changes

- Archived the retired NoneBot2 runtime and old AI conversation path under `ai_plugin/legacy_nonebot2/`.
- Replaced `ai_plugin/ai_plugin/__init__.py` with a side-effect-free package initializer so AstrBot imports such as `ai_plugin.news` no longer load the old NoneBot2 router.
- Added `ai_plugin/legacy_nonebot2/README.md` documenting that the archived files are reference-only and not part of the active AstrBot path.
- Updated `README.md`, `AGENTS.md`, and `CLAUDE.md` to point to the archive and describe `ai_plugin` as a reusable module library.

## Archived Files

- `ai_plugin/bot.py`
- `ai_plugin/nonebot2.toml`
- `ai_plugin/pyproject.toml`
- `ai_plugin/ai_plugin/__init__.py`
- `ai_plugin/ai_plugin/router.py`
- `ai_plugin/ai_plugin/handler.py`
- `ai_plugin/ai_plugin/hermes_client.py`
- `ai_plugin/ai_plugin/mcp_tools.py`
- `ai_plugin/ai_plugin/mcp_history.py`
- `ai_plugin/ai_plugin/tools.py`

## Verification

- Ran a minimal import check through `astrbot_venv\Scripts\python.exe`.
- Verified the lightweight `ai_plugin` package imports without pulling the old NoneBot2 entry.
- Verified common reused modules still import: `news`, `weather`, `epic`, `kfc`, `oilprice`, `bilibili_dynamic`, `image_generator`, `image_editor`.

## Notes

- Existing unrelated dirty submodule changes were left untouched.
- Some active business modules still import `nonebot` for logger/scheduler compatibility through `_nonebot_stubs`; those were intentionally not refactored in this archive-only pass.

## Additional Changes

- Added live group-chat interjection support to `data/plugins/astrbot_plugin_proactive_chat`.
- The bot now has a separate `group_settings.live_interject_settings` config block for active-chat interjections:
  - `enable_live_interject`: default enabled for already configured group proactive sessions.
  - `probability_percent`: default `1`, meaning a 1% chance per ordinary group message.
  - `cooldown_seconds`: default `300`, preventing repeated interjections in busy groups.
  - `prompt`: instructs the LLM to understand recent context and reply to the last message before the interjection.
- Live interjections reuse the existing context builder, LLM provider path, message sender, and conversation archive flow without changing the idle/silence proactive-message counters.
- Added the new config block to session override sanitization and the admin config renderer order.

## Additional Verification

- Ran `astrbot_venv\Scripts\python.exe -m py_compile` for the modified proactive-chat Python files.
- Parsed `data/plugins/astrbot_plugin_proactive_chat/_conf_schema.json` with Python `json.load`.

## Git Note

- `data/` is ignored by the main repo via `.gitignore`, so `data/plugins/astrbot_plugin_proactive_chat` changes are local runtime/plugin changes and do not appear in normal `git status`.

## Image Generation Latency Check

- Investigated a slow image-generation request that took about 64 seconds between "正在画" and final image send.
- Current image config uses `GPT_IMAGE_MODEL=gpt-image-2` through `https://api.gemai.cc`; this path has a 300-second HTTP timeout and can spend around a minute waiting for the proxy/model to finish.
- Added timing logs in `ai_plugin/ai_plugin/image_generator.py` to split image latency into API request time and download/save time.
- Added total elapsed logging in `data/plugins/jiang_image/main.py` for the AstrBot image command wrapper.
- Verified both modified files compile with `astrbot_venv\Scripts\python.exe -m py_compile`.

## Group Daily Analysis Nickname Fix

- Identified the report image as generated by `data/plugins/astrbot_plugin_qq_group_daily_analysis`.
- Fixed OneBot member lookup for WeChat chatrooms in `src/infrastructure/platform/adapters/onebot_adapter.py`.
- `get_group_member_list` now uses a string group id for `@chatroom` sessions and handles both direct-list and `{"data": [...]}` / `{"member_list": [...]}` response shapes.
- Member display names now prefer `card` / `display_name`, then `nickname`, and fall back to wxid only when no friendly name is available.
- Historical messages whose sender name is a placeholder (`wxid_*`, empty, unknown, or equal to sender_id) are rewritten with the resolved group nickname before analysis/report generation.
- Verified the adapter compiles and ran a fake bot mapping check for `wxid -> 群昵称`.

## Live Interjection Freshness Guard

- Added `max_response_delay_seconds` to `group_settings.live_interject_settings`, defaulting to 120 seconds.
- Random live interjection now skips messages that are already older than the configured freshness window.
- New group messages during LLM generation no longer cancel the interjection as long as the reply is still inside the freshness window.
- If generation itself exceeds the freshness window, the reply is discarded to avoid responding to stale context.
- Verified `message_events.py` compiles and `_conf_schema.json` parses successfully.

## WeChat Recall Queue Fix

- Reworked `Py/main.py` bot-message recall tracking for repeated `图来` image sends.
- Added trace tracking for text/image sends and captured DLL `11047` send responses to store the real WeChat `newMsgId`.
- Changed `recall_last_msg` to consume a per-chat FIFO queue of sent messages, so overlapping delayed recall tasks no longer overwrite each other or empty the single latest-message slot.
- Added the missing `helper_recall_msg` type `11244` sender and kept `@chatroom` group id fallback for stripped group ids.
- Verified with `Python311-32\python.exe -m py_compile E:\Project\Py\main.py`.

## WeChat WebSocket Reconnect Guard

- Investigated a reconnect after group analysis report rendering: `Py/main.py` logged WebSocket close at 15:31:18 and reconnected at 15:31:21.
- Added connection-loop guards in `Py/main.py` so only one connect attempt, receive loop, and auto-reconnect task can run at a time.
- Disabled websocket protocol ping on the reverse WS client and kept the existing V11 heartbeat path.
- Verified with `Python311-32\python.exe -m py_compile E:\Project\Py\main.py`.
