# 2026-07-27 开发日报

## 金山文档名单同步排查

- 确认 `jiang_raid_signup` 的金山名单同步已启用，原运行窗口为北京时间每周一 11:00–19:30，每 10 分钟执行一次。
- 生产 Nginx 历史日志显示 2026-07-20 的 AirScript 在同步窗口内持续调用 `/api/kdocs/events/weekly-roster` 并返回 HTTP 200，19:30 封存接口也返回 HTTP 200，说明上周脚本和网站接口正常。
- 原因定位为活动名称不一致：网站活动已使用“双十”，旧 AirScript 和服务器环境仍使用“双10”，完全匹配活动名时会定位到不同活动。
- 早前出现的盛世提醒接口 `TimeoutError` 属于连接超时；插件已有一次重试逻辑，与金山文档 B/C 列同步问题不是同一故障。

## “双十”活动预检查与同步改造

- `jiang_raid_signup` 新增 `kdocs_event_title`（默认“双十”）和 `kdocs_event_ensure_time`（默认 10:50）配置。
- 每周一 10:50–11:00 通过盛世机器人接口检查当天同名活动：存在则复用，不存在才创建；同一运行周期只成功执行一次。
- 11:00 起的名单同步和 19:30 的封存清空均显式传递活动名称“双十”。
- 盛世网站新增 `POST /api/bot/raid/ensure`，提供幂等的同日同名活动创建/复用能力。
- 管理后台生成的 AirScript 会按当前活动名称生成，不再只替换 Token。
- AirScript 支持调用参数 `eventTitle` 和 `ensure_event`，默认活动名改为“双十”。
- 活动结束时清空范围确认并统一为 `B3:C12`；B 列继续作为网站名单同步来源，C 列只在结束时一并清空。

## 旧脚本兼容与生产部署

- 生产环境 `KDOCS_EVENT_TITLE` 已改为“双十”。
- 网站的金山周活动接口新增兼容映射：旧脚本传“双10”时按“双十”处理，避免 11:00 误建重复活动。
- 生产环境已确认今天的“双十”活动 ID 为 7；预检查接口返回 `created=false`，说明直接复用现有活动。
- 使用旧标题“双10”读取生产金山接口，返回活动 ID 7、标题“双十”，兼容逻辑生效。
- 生产网站 `shengshi` 已通过 PM2 reload 应用代码，状态 online，公网健康检查 HTTP 200。
- 生产改动前文件备份位于 `/var/www/shengshi/.codex-backups/kdocs-double-ten-before-20260727-1019`。

## 验证

- `jiang_raid_signup`：37 个 `unittest` 测试全部通过。
- `main.py` Python 编译检查通过。
- 插件配置和配置 Schema 使用 UTF-8 解析通过。
- 盛世网站：4 个 Node 测试全部通过，包含旧标题兼容、幂等创建/复用、动态脚本活动名和 `B3:C12` 清空断言。
- 尝试通过 WebUI 热加载插件时，配置文件中的旧登录凭据被拒绝；未猜测密码、未反复登录，也未启动、停止或重启 AstrBot 与 `Py/main.py`。日志显示插件随后在 10:58 被热加载为 2.6.0，但原生命周期只在平台首次加载时启动后台循环，因此 11:00 未发出同步请求；已补充 `initialize` 与插件加载完成钩子，后续热加载也会恢复定时任务。
- 11:00 后数据库仍只有活动 ID 7 的“双十”，未生成“双10”重复活动。
- 手动执行一次只同步、不清空的 Webhook 验证时，AirScript 返回 `Identifier 'EVENT_TYPE' has already been declared`。这证明此前页面编辑器自动保存了重复尾部代码；名单和表格未被改写，但云端脚本当前不可执行。
- 尝试恢复云端脚本时用户按 Esc 停止浏览器控制，已立即停止后续页面操作。云端脚本仍需手动恢复为网站后台生成的已测试版本。

## B站动态推送样式优化

- 将燕云十六声动态改为封面图优先发送，精简文字随后发送，避免大段文字遮住视觉重点。
- 每次只输出最新一条动态；正文只保留第一条有效句子，标题摘要最长 64 字，删除重复作者信息和方括号类型标记。
- 统一修改自然语言能力路由、`/燕云` 命令和定时订阅三个发送入口，均使用“图片 → 文字”的顺序。
- 自动化验证通过：38 个测试、4 个子测试全部通过；相关 Python 文件编译检查和 `git diff --check` 通过。
- 通过本机 Dashboard API 热重载 `jiang_capability_router`、`jiang_commands`、`jiang_schedule`，三个请求均返回 HTTP 200 / `ok`；重载日志未发现错误。未启动、停止或重启 AstrBot 与 `Py/main.py`。

## WeChat 表情包发送兼容修复

- 定位到 `meme_manager` v4 将表情图库从旧的 `memes/` 迁移到 `packs/<pack_id>/memes/`，原微信桥接路径判断不再命中。
- 同时确认更新后的 AstrBot `aiocqhttp` 适配器会把所有图片转为 base64，导致本地路径与表情子类型在到达 `Py/main.py` 前丢失。
- 适配器现在对 meme_manager 新旧目录保留本地文件 URI，并显式添加 `sub_type=emoji`；`Py/main.py` 也同时兼容两种目录结构，继续通过微信 CDN 表情接口 `type=11254` 发送。
- 新增 3 个针对性测试，覆盖旧目录、v4 packs 目录、普通图片排除，以及 AstrBot 到微信桥接的完整表情分类链路。
- AstrBot 侧单元测试全部通过；适配器编译检查通过；`Py/main.py` 与测试文件使用 Python 3.11 32-bit 编译检查通过。未启动、停止或重启 AstrBot 与 `Py/main.py`。

## 未知斜杠消息拦截

- 保留 AstrBot 顶层 `/` 唤醒前缀，避免 `@filter.command` 注册的现有命令失效。
- 在 `jiang_capability_router` 的 LLM 请求前置钩子中新增高优先级守卫：原始消息以 `/` 开头、且没有被命令或其他插件处理时，静默终止默认 LLM 回退。
- 命令插件显式创建的 `ProviderRequest` 会被放行，因此需要大模型参与的合法命令不受影响；普通 `@机器人` 聊天也不受影响。
- 新增 `block_unknown_slash_llm` 配置项，默认启用，可在插件配置中关闭；插件版本更新为 1.1.0。
- 能力路由插件 41 个测试全部通过，Python 编译、配置 Schema JSON 解析和差异格式检查通过。
- 本机内置浏览器和 Chrome 均没有 AstrBot Dashboard 登录态，因此未猜测密码、未修改认证配置，也未热重载插件。未启动、停止或重启 AstrBot 与 `Py/main.py`。

## 微信引用图片缓存清理

- 微信桥接层新增引用图片缓存清理任务：`Py/data/quoted_images` 中的 `quoted_*` 图片保留 3 天，启动时检查一次，随后每小时检查。
- 清理范围只包含引用图片的 JPG、JPEG、PNG、WebP 和 GIF 文件，不影响表情包、生图结果或其他插件文件；桥接服务退出时会通知清理线程停止。
- 已按同一规则清理现有缓存：删除 156 个超过 72 小时的文件，释放 66.67 MB；保留最近 3 天的 9 个文件，共 5.51 MB。
- 新增 2 个针对性测试；微信桥接层 15 个 Python 3.11 32-bit 测试和 3 个 AstrBot 兼容测试全部通过，编译检查及差异格式检查通过。
- 未启动、停止或重启 AstrBot 与 `Py/main.py`；定时任务会在微信桥接层下次正常启动时生效。

## 复读机智能过滤

- `jiang_repeater` 现在只让纯文本普通聊天参与复读；`@` 消息、唤醒/管理命令、点歌命令、引用与转发、卡片 XML、媒体消息和机器人自身消息都会被排除。
- 不可复读消息会清空该群的候选历史，避免普通消息跨命令、卡片或机器人回复误判为“连续两条”；两条候选还必须由不同用户在 60 秒内连续发出。
- 针对本次两类误触发补充测试：两个用户重复 `@姜小妹` 不再触发，两个外部机器人返回相同音乐卡片时，`qt/qs/qsw/qtxt` 引用元数据也不会被复读。
- 插件版本更新为 1.1.0；10 个单元测试全部通过，Python 编译检查和差异格式检查通过。
- 尝试通过本机 Dashboard API 热重载时返回 HTTP 401，未猜测密码或修改认证配置；未启动、停止或重启 AstrBot 与 `Py/main.py`。
