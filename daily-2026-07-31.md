# 2026-07-31 开发日报

## 修复 BitTorrent 磁力搜索

- 定位 `/bt` 失败并非命令格式错误：旧磁力狗站点 `clg.clgapp4.xyz` 的 443 端口已无法连接，插件捕获到空文本的 `httpx.ConnectError`。
- 将搜索站点更新为当前可用的 `https://clg02.cc/search`，适配新版 Base64 搜索结果、`/information/` 详情页和磁力链接提取，同时保留旧版 `cllj.php` / `xq.php` 解析兼容。
- 修复“无结果”和“搜索失败”分支只构造消息、不发送消息的问题。
- `/bt` 与 `/btp` 现在会终止后续事件传播，避免插件处理后普通 AI 再接管并错误提示“命令前面没斜杠”。
- 修复多词关键词被误当成排序参数的问题；只有明确的“热门/最新/大小/相关度”等参数才启用排序。
- 为连接失败、超时和 HTTP 状态异常补充可读日志与用户提示，空异常现在会记录具体异常类型。
- 使用 AstrBot Python 3.13 环境完成 `py_compile`、命令参数/事件停止模拟验证，并以英文和中文关键词实测新版站点搜索及详情页磁力提取成功。
- 未启动、停止、重启 AstrBot 或 `Py/main.py`，也未热重载插件。
