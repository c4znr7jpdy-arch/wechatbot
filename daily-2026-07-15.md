# 2026-07-15 开发日报

## 米游币任务下线适配

- 用户确认米游社已取消阅读、点赞、分享任务。
- 使用当前绑定账户只读验证米游社正式接口：`getMissions` 仅返回 `continuous_sign / 打卡 / threshold=1 / points=30`，`getUserMissionsState` 也只包含 `continuous_sign`，确认三项任务确已从任务清单下线。
- 删除 AstrBot 适配层“固定执行签到、阅读、点赞、分享”的行为，改为严格根据米游社实时返回的 `state_dict` 动态选择任务。
- `/任务` 和登录后、每日 00:01 自动任务现在只会执行及展示仍存在的签到任务，不再请求帖子、阅读、点赞或分享。
- 保留账户隔离、每日缓存、网络三次重试、未完成时每两分钟补跑（最多五次）、私聊 UMO、00:01 调度和错过时间后的补跑机制。
- 回归验证通过：真实任务列表与状态只读请求、仅签到场景、已下线三项不调用、结果排版和 Python 语法检查。
- `jiang_mystool` 升级至 v2.7.0；未启动、停止或重启 AstrBot 与 `Py/main.py`。

## 燕云十六声小程序自动签到

- 分析 `抓包文件/未命名文件夹` 中的 HTTP 会话，并解包本机微信小程序 `wx3f78b6f96e5992f4` 413 版代码，确认服务基址为 `https://s3.game.163.com/7540694694f2dddc`。
- 确认签到链路：`/integral/calendar/fig` 查询日历、`/integral/cumulative/fig` 查询累计奖励、`/user/sign` 执行签到；请求只需携带 `access_token`。
- 使用现有 Token 完成一次真实签到，获得 `粗矿石*10`；复查 2026-07-15 的 `isSign=true`。
- 新增 AstrBot 插件 `jiang_yanyun_checkin`：每天 08:30（Asia/Shanghai）自动签到，签到前先查状态以避免重复请求，签到后再次复核结果。
- 支持 `/燕云签到`、`/燕云签到状态` 管理员命令，成功/失败通知、通知目标配置、网络重试、状态记录以及 Token 到期前每日提醒。
- Token 可直接配置，也可从抓包请求文本自动提取；当前本地配置引用 `[158] session_s3.game.163.com_message.txt`，仓库插件代码及配置中未复制 JWT 明文。
- 验证通过：Python 编译与模块导入、配置 JSON、Cron 注册/卸载生命周期、通知 UMO 解析、真实线上状态查询和已签到幂等路径；未启动、停止或重启 AstrBot 与 `Py/main.py`。

## 燕云鹅毛商城任务自动化

- 解析鹅毛商城活动 `3NSsCV` 的登录、签名、任务状态、任务领取和积分接口，实现动态签名与小程序 Token 到商城会话的登录链路。
- 还原商城任务状态机：`0=未接取`、`1=未完成`、`2=待领取`、`3=已领取`；修正了早期把 `take` 和 `complete` 用反的问题。
- 根据新增抓包实现“每日查看或分享江湖情报”：读取帖子列表第一条，再请求帖子详情并等待商城任务进度同步。
- 实现“每日收藏或分享万相集作品”：从独立作品接口分页选择未收藏作品，通过 `v=1` 收藏，避免对已收藏作品执行反向操作。
- 每日游戏登录和每周数据工具任务会检测真实外部状态；条件满足后自动领取，未满足时提示需要游戏或微信小程序侧实际完成。
- 根据 `抓包文件/分享` 和商城前端任务组件确认“每周邀请好友查看积分商城”只要求触发小程序页面分享：插件现会模拟分享回调并自动领取，无需好友实际访问。线上验证用户分享后状态为待领取，领取成功后鹅毛 `290 → 300`。
- 新增 `/燕云鹅毛任务`、`/燕云鹅毛状态`、`/燕云全部任务`，并纳入每日签到后的定时任务与通知结果。
- 优化微信任务消息排版：采用 `①～⑥ + 状态图标 + 任务名` 的统一结构，已领取项只显示绿色 `✅`，人工条件说明缩进换行，累计签到也纳入编号序列。
- 真实线上验证：数据工具任务领取后鹅毛 `260 → 270`；江湖情报和万相集各领取 10，鹅毛 `270 → 290`；第二次执行保持 290，确认领取幂等。
- 插件升级至 v1.2.1；未启动、停止或重启 AstrBot 与 `Py/main.py`。

## 燕云多人版需求与交接

- 输出 `docs/requirements/requirements-yanyun-multi-user.md`，明确多人绑定、Token 加密隔离、用户级状态、单 Cron 有限并发、个人通知、管理员管理和旧账号迁移方案。
- V1 采用用户私聊提交自己抓取的 Token；确认官方小程序身份限制使机器人无法在服务端替其他微信账号无感生成 Token，自动取 Token 作为独立后续项目。
- 给出 SQLite 数据模型、命令清单、实施顺序、风险边界和可执行验收标准，供新会话直接实现。

## 燕云插件多人版实现

- 将 `jiang_yanyun_checkin` 升级至 v2.0.0，新增 `models.py`、`store.py`、`crypto.py`、`account_service.py` 和 `scheduler.py`，把单一全局账号改为以 `(platform_id, sender_id)` 隔离的 SQLite 多用户账号、凭据和任务状态。
- Token 使用独立 Fernet 密钥加密落盘，只保存 SHA-256 前 12 位指纹；密钥文件应用仅当前服务账户可访问的权限，日志、状态和回复统一执行 JWT/查询参数脱敏。
- 新增用户私聊绑定、5 分钟两步绑定、二次确认解绑、个人状态、个人签到/鹅毛/全部任务及通知开关；群聊中的敏感命令只提示转私聊。
- 所有手动命令改为读取 `event.get_sender_id()` 对应账号；固定管理员 wxid `fengchenhao002` 可查看脱敏用户列表、启用/禁用、二次确认删除及触发全用户批处理，但不能查看 Token 明文。
- 每日 Cron 改为全用户批处理：默认并发 3、账号间随机抖动、同账号互斥、单账号错误隔离、个人结果按私聊 UMO 分发，并向管理员发送无隐私明细的批次汇总；鹅毛商城为每次账号操作使用独立 Cookie 会话，避免并发登录态串号。
- 首次加载会验证旧 `access_token` / `token_source_file` 后迁移为管理员账号，并迁移旧 `state.json`；旧配置和状态文件保持不变，新定时路径只使用多人数据库。
- WebUI schema 移除旧 Token 明文字段及旧全局通知目标，新增最大并发、自助绑定和仅私聊绑定策略；README 已更新多人命令、安全边界、数据路径和回滚说明。
- 离线验证通过 6 项：加密落盘、双用户隔离、解绑不影响他人、同用户并发串行、失效 Token 批次隔离、旧状态迁移、JWT 脱敏及插件 Cron 初始化/卸载生命周期；Python 编译、模块导入和配置 JSON 校验通过。
- 未启动、停止或重启 AstrBot 与 `Py/main.py`，未执行真实燕云写操作。
