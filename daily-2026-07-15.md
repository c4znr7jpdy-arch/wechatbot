# 2026-07-15 开发日报

## 米游币任务下线适配

- 用户确认米游社已取消阅读、点赞、分享任务。
- 使用当前绑定账户只读验证米游社正式接口：`getMissions` 仅返回 `continuous_sign / 打卡 / threshold=1 / points=30`，`getUserMissionsState` 也只包含 `continuous_sign`，确认三项任务确已从任务清单下线。
- 删除 AstrBot 适配层“固定执行签到、阅读、点赞、分享”的行为，改为严格根据米游社实时返回的 `state_dict` 动态选择任务。
- `/任务` 和登录后、每日 00:01 自动任务现在只会执行及展示仍存在的签到任务，不再请求帖子、阅读、点赞或分享。
- 保留账户隔离、每日缓存、网络三次重试、未完成时每两分钟补跑（最多五次）、私聊 UMO、00:01 调度和错过时间后的补跑机制。
- 回归验证通过：真实任务列表与状态只读请求、仅签到场景、已下线三项不调用、结果排版和 Python 语法检查。
- `jiang_mystool` 升级至 v2.7.0；未启动、停止或重启 AstrBot 与 `Py/main.py`。

## 燕云十六声小程序自动签到

- 分析 `抓包文件/未命名文件夹` 中的 HTTP 会话，并解包本机微信小程序 `wx3f78b6f96e5992f4` 413 版代码，确认服务基址为 `https://s3.game.163.com/7540694694f2dddc`。
- 确认签到链路：`/integral/calendar/fig` 查询日历、`/integral/cumulative/fig` 查询累计奖励、`/user/sign` 执行签到；请求只需携带 `access_token`。
- 使用现有 Token 完成一次真实签到，获得 `粗矿石*10`；复查 2026-07-15 的 `isSign=true`。
- 新增 AstrBot 插件 `jiang_yanyun_checkin`：每天 08:30（Asia/Shanghai）自动签到，签到前先查状态以避免重复请求，签到后再次复核结果。
- 支持 `/燕云签到`、`/燕云签到状态` 管理员命令，成功/失败通知、通知目标配置、网络重试、状态记录以及 Token 到期前每日提醒。
- Token 可直接配置，也可从抓包请求文本自动提取；当前本地配置引用 `[158] session_s3.game.163.com_message.txt`，仓库插件代码及配置中未复制 JWT 明文。
- 验证通过：Python 编译与模块导入、配置 JSON、Cron 注册/卸载生命周期、通知 UMO 解析、真实线上状态查询和已签到幂等路径；未启动、停止或重启 AstrBot 与 `Py/main.py`。
