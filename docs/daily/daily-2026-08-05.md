# 2026-08-05 开发日报

## 短剧实时更新核验

- 确认 `jiang_short_drama` 1.2.4 不保存离线片库，每次搜索实时访问 8 个 HTTPS 苹果 CMS/VOD 源。
- 逐源请求第一页数据，8 个来源均返回 HTTP 200；当天最新资源时间达到 2026-08-05 09:44，量子资源第一页包含多部当天入库并已完结的短剧。
- 明确区分“资源站实时更新”与“官方平台绝对同步”：资源仍可能因上游延迟、缺片或下架而晚于官方平台。

## “短剧推荐”精确命令

- 新增完全匹配命令 `短剧推荐`；前后空格、`/` 前缀、额外文字以及 `短剧 推荐` 均不会触发推荐分支。
- 推荐器并发读取 8 个资源站的最新详情页，根据资源站分类树识别“短剧/漫剧”及其子分类，只保留含 HLS m3u8 播放线路的条目。
- 跨来源按归一化剧名去重，再按资源更新时间倒序返回 20 部；机器人消息只包含剧名，每行一部、不加序号，便于用户复制。
- 推荐结果缓存 10 分钟；不足 20 部时最多读取各源前三页，兼顾实时性与上游请求压力。
- 新增精确命令匹配、短剧子分类识别、非 HLS 与非短剧过滤测试；25 项单元测试、Python 语法及差异检查通过。实时烟测成功返回 20 个不重复短剧剧名。
- 已在 AstrBot 插件管理页将 `jiang_short_drama` 热重载至 1.3.0，后台提示重载成功；未重启 AstrBot 或微信注入进程。

## 同名多版本选择

- 新增 `短剧 剧名 版本` 候选查询和 `短剧 剧名 版本N` 指定播放；普通播放卡片增加换版本提示，并在摘要中展示年份和类型。
- 候选列表展示实际资源名、年份、类型、集数、主要演员和来源，按标题、年份、媒介类型及主演跨源去重；自动合并“国产动漫/中国动漫”等同义分类，过滤解说、预告和花絮。
- 实时查询《三国演义》得到 5 个有效版本，包括 1994 年 84 集电视剧和 2010 年 95 集电视剧；30 项单元测试通过。
- 已将 `jiang_short_drama` 热重载至 1.3.1，AstrBot 后台提示重载成功；未重启 AstrBot 或微信注入进程。

## 多版本封面中间页

- 普通搜索发现两个及以上有效版本时，改为发送一张“多个版本”微信卡片；点击进入响应式版本中间页，以封面卡片展示资源名、年份、类型、集数、主演和来源。
- 点击任一版本后由服务端把版本令牌转换为播放令牌并跳转到原有播放器，卡片和中间页均不嵌入 m3u8 地址；单版本搜索、指定分集和公网播放器不可用时保持原有降级流程。
- 普通搜索改为单次采集全部匹配项，同时选择最佳结果并缓存版本候选，避免为歧义检测重复请求 8 个资源站；《三国演义》实时烟测约 2.2 秒得到 5 个版本。
- 使用真实封面完成桌面视觉检查，验证点击 1994 年版本可跳转到 84 集播放器；33 项单元测试通过。
- 已将 `jiang_short_drama` 热重载至 1.4.0，后台提示重载成功；公网播放器健康检查返回正常，未重启 AstrBot 或微信注入进程。

## 体育回放分页集合

- 新增 `CBA2023`、`NBA2024`、`英超2025` 等“联赛 + 年份”集合识别，使用带空格的上游关键词查询后在本地同时校验联赛和年份，避免混入仅命中年份的普通影视。
- 赛事卡片进入独立回放中间页：第一页沿用搜索阶段缓存，封面原生懒加载；滚动接近底部或点击按钮时才读取下一页，空白页停止自动连翻，支持逐页访问上游全部结果而不阻塞首屏。
- 新增集合令牌、分页 JSON 接口和回放选择跳转；跨源、单页和已加载页面之间按归一化标题去重，卡片及集合页不嵌入 m3u8 地址。
- 实测 `CBA2023` 首屏约 2.25 秒返回 40 场，第二页约 1.3 秒返回 40 场；本地端到端验证集合页、两页 API 和回放到播放器的 302 跳转正常，38 项单元测试通过。
- 已将 `jiang_short_drama` 热重载至 1.5.0，后台提示重载成功；公网播放器健康检查正常，未重启 AstrBot 或微信注入进程。

## 无年份体育分类搜索

- 体育集合命令扩展为年份可选，支持 `短剧 斯诺克`、`短剧 足球`、`短剧 篮球` 等分类词，并补充台球、网球、羽毛球、乒乓球、排球、F1、UFC、WWE 等关键词。
- 除标题关键词和可选年份外，新增资源站类型校验及分类别名映射；斯诺克兼容“斯诺克/台球”，足球仅接受足球分类，过滤剧情片、综艺、动漫和短剧中的同名词。
- 实测斯诺克首屏约 1.44 秒返回 62 场、足球首屏约 1.05 秒返回 43 场，均保留按需分页；39 项单元测试通过。
- 已将 `jiang_short_drama` 热重载至 1.6.0，后台提示重载成功；公网播放器健康检查返回正常，未重启 AstrBot 或微信注入进程。

## 短剧与体育命令拆分

- 将体育回放从 `短剧 ` 入口迁移到独立的 `体育 ` 强匹配命令；`短剧 xxx` 只处理影视/短剧，`体育 斯诺克`、`体育 足球`、`体育 CBA2023` 等进入赛事回放集合页。
- 单独发送 `体育`、不带空格的 `体育足球`、带 `/` 前缀或使用全角空格均不触发插件，避免抢占普通聊天。
- 新增完整赛事名称直达：仅在资源站标题完全一致且分类属于体育时发送可播放卡片；部分标题、近似标题以及同名剧情片/动漫不会直达。
- 实时烟测从斯诺克集合首屏取得 62 场回放，再以完整标题精准查询，成功返回同名斯诺克资源及 16 条播放线路；42 项单元测试和 Python 语法检查通过。
- 已将 `jiang_short_drama` 热重载至 1.7.0，AstrBot 后台提示重载成功；未重启 AstrBot 或微信注入进程。

## 微信播放卡片精简排版

- 普通短剧播放卡片移除重复的“短剧”标题前缀，标题调整为“剧名｜当前集/完整版”。
- 摘要仅保留年份、类型、总集数或完整版时长和“点击播放”；移除资源站名称、当前集重复信息、播放器选集说明及其他版本命令，避免微信小卡片文字拥挤。
- 新增分集卡片和完整版卡片格式测试；播放器选集、多版本选择及播放流程保持不变。
- 完整版仍保留实际探测得到的约分钟数；分集资源缺少可靠时长时显示总集数，不生成估算时长。
- 已将 `jiang_short_drama` 热重载至 1.8.0，AstrBot 后台提示重载成功；未重启 AstrBot 或微信注入进程。

## 多版本卡片精简排版

- 多版本卡片标题从“短剧｜剧名｜多个版本”精简为“剧名｜N个版本”。
- 摘要不再逐项拼接“年份+类型”，仅展示去重、按时间排序的年份和“点击选择版本”；封面、类型、演员和来源继续在版本选择页完整展示。
- 新增《红楼梦》6版本格式回归测试，预期输出为“红楼梦｜6个版本”和“1987、2010、2018 · 点击选择版本”。
- 已将 `jiang_short_drama` 热重载至 1.9.0，AstrBot 后台提示重载成功；未重启 AstrBot 或微信注入进程。

## 分类影视命令与严格类型过滤

- `短剧 ` 改为只接受短剧分类；新增 `电视剧 `、`电影 `、`动漫 `、`综艺 ` 强匹配命令，分别过滤电视/连续剧、电影、动漫/动画和综艺类资源站结果。
- 分类校验前置到每个资源站候选进入评分池之前，精准搜索、模糊二次查询、同名多版本和版本缓存均携带媒体类型，防止同名电影、电视剧、动漫、综艺及短剧相互串线。
- 精确命令 `剧` 输出所有影视分类、体育回放、短剧推荐、指定分集和版本选择帮助；单独的分类名、无 ASCII 空格、带 `/` 前缀或全角空格均不触发分类搜索。
- 实时烟测通过：`短剧 重回末日前` 返回短剧分类，`电视剧 三国演义` 返回国产剧84集，`电影 流浪地球` 返回科幻片，`动漫 海贼王` 返回日本动漫1178集，`综艺 奔跑吧` 返回大陆综艺84集。
- 同名版本烟测中，《三国演义》的电视剧候选仅含国产剧、动漫候选仅含中国/国产动漫，电影分类无结果且未跨类型回退，确认版本页同样执行严格分类隔离。
- 已将插件说明更新为“分类影视、体育资源搜索与微信播放卡片”，并将 `jiang_short_drama` 热重载至 2.0.0；AstrBot 后台提示重载成功，未重启 AstrBot 或微信注入进程。

## 全分类搜索与短剧推荐中间页

- 在精确帮助命令 `剧` 之外新增 `剧 名称` 强匹配格式；该格式不做媒体分类过滤，可命中短剧、电视剧、电影、动漫、综艺、体育及其他未知分类，分类搜索命令仍保持严格过滤。
- `短剧推荐` 从20行纯文本改为一张“最新短剧推荐｜12部”微信卡片；点击进入令牌保护的推荐中间页，以真实封面展示12部最新可播放短剧，点击作品直接转换为播放令牌并进入播放器。
- 推荐采集覆盖短剧及 AI 漫剧分类；跨源按归一化标题去重、按资源站更新时间倒序，保留10分钟缓存，卡片及中间页不携带 m3u8 地址。
- 实时烟测取得12部短剧、12张封面且全部含可播放线路；`剧 三国演义` 可在无分类过滤下命中中国动漫，证明通用搜索覆盖已有及其他分类。
- 已将 `jiang_short_drama` 热重载至 2.1.0，AstrBot 后台提示重载成功；公网播放器健康检查正常，52 项单元测试、Python 语法检查及差异检查通过，未重启 AstrBot 或微信注入进程。

## 影视帮助文本排版与 AI 漫剧归类

- `剧` 保持直接回复文本，不进入帮助页；帮助内容改为带少量图标的四段结构：分类搜索、体育回放、最新推荐、选集与版本，并将命令示例独立成行，修正旧截图中“推荐20部”的过期文案。
- `AI漫剧` 正式归入短剧分类，`短剧 名称` 的严格分类搜索和 `短剧推荐` 的最新12部采集均允许命中。
- 已将 `jiang_short_drama` 热重载至 2.1.1，后台提示重载成功；52 项单元测试、Python 语法检查、帮助文本实输检查及公网播放器健康检查均通过，未重启 AstrBot 或微信注入进程。

## 群聊影视定时推荐 WebUI

- `jiang_short_drama` 新增“影视定时推荐”插件页面，可按群聊 UMO 管理主动推送任务；支持短剧（含 AI 漫剧）、电视剧、电影、动漫、综艺、体育回放和全部分类，可设置每天或指定星期、发送时间、每次 6-20 部及启停状态。
- 任务持久化到 `data/plugin_data/jiang_short_drama/recommendation_schedules.json`，保存、编辑、启停和删除会立即同步 AstrBot Cron；定时回调实时采集相应分类最新资源，通过 UMO 主动发送微信链接卡片，推荐中间页标题随分类变化。
- WebUI 提供“立即测试”，不改变计划时间；使用 `aiocqhttp:GroupMessage:51632940287@chatroom` 成功发送 12 部最新短剧卡片。另创建 23:58 启用任务并在 AstrBot“未来任务”中确认 Cron 注册成功，随后停用、删除，确认测试任务和 Cron 均已清理。
- 完成桌面宽屏页面、空状态、新建表单、任务卡片及删除确认的视觉与交互检查；57 项单元测试、Python/JavaScript 语法、公网播放器健康及差异检查通过。插件已热重载至 2.2.0，未重启 AstrBot 或微信注入进程。

## 短剧推荐首条封面兼容

- 普通 `短剧推荐` 和 WebUI 定时推荐卡片统一固定使用推荐列表第 1 条资源的封面；第一条缺图时使用默认图，不再跳到第二条借用封面。
- 新增短期封面令牌接口，将资源站的 WebP/AVIF 等封面按需转换并缓存为 JPEG，解决微信链接卡片显示通用链条图标的问题；仅小封面经过机器人服务器，视频流仍由播放源直连。
- 使用当前首条推荐《我当卧底这件事》的真实 WebP 完成本地端到端烟测，接口返回 HTTP 200、`image/jpeg` 和有效 JPEG 文件头；公网播放器健康检查正常。
- 60 项单元测试、Python 语法检查及差异检查通过；插件已热重载至 2.2.1，后台显示“重载成功”，未重启 AstrBot 或微信注入进程。

## 播放器全屏按钮避让

- 将原先悬浮在播放器右下角、会遮挡浏览器原生全屏与更多菜单的自定义全屏按钮移到右上角。
- 清晰度与全屏组成紧凑按钮组，呈现为“原画/清晰度 + 圆形全屏图标”；普通播放和全屏播放位置保持一致，不影响左上角选集及正中当前集数。
- 增加结构回归断言，确认页面只有一个自定义全屏按钮、存在右上角按钮组且旧的 `bottom: 54px` 定位已移除；60 项单元测试和 Python 语法检查通过。
- 插件已热重载至 2.2.2，后台显示“重载成功”，未重启 AstrBot 或微信注入进程。

## 长按倍速与全端首屏居中布局

- 新增视频画面长按手势：播放中按住约 350 毫秒临时切换到 2 倍速，松手或取消触摸后恢复长按前的实际倍速。
- 倍速提示从画面正中移到左上角选集按钮下方，缩减为半透明 `2×` 小标记；进入倍速约 1.2 秒后自动隐藏，即使继续按住也不遮挡主体画面。
- 手势排除视频底部原生控制栏区域，拖动进度条、调整音量或点击原生按钮不会误触；长按结束后短暂抑制随后的点击，避免意外暂停。
- 桌面与手机端均将标题、播放器和播放状态组合为首屏舞台并水平、垂直居中；手机使用 `visualViewport` 的实际可见高度，微信地址栏伸缩、刘海屏及横竖屏变化后会动态重新计算，选集始终紧跟播放器首屏之后。
- 播放器脚本缓存版本更新至 2.3.1；62 项单元测试、HTML 标签平衡、Python 语法和差异检查通过。插件已热重载成功，公网脚本确认包含 `visualViewport` 动态居中、视口变化重新计算、提示 1.2 秒自动隐藏及松手恢复倍速逻辑，未重启 AstrBot 或微信注入进程。

## 播放位置跨卡片续播

- 播放器历史由短期随机播放令牌改为“剧名 + 资源来源 + 封面”的稳定身份；重新搜索、从多版本页或短剧推荐页打开同一资源时，可以恢复上次集数和各集播放秒数。
- 播放中每约 5 秒、拖动进度后、切换集数、页面隐藏和离开页面都会保存进度；接近本集结尾或播放结束会清除该集进度，自动下一集不会从旧位置开始。
- 普通卡片默认优先续播；用户明确发送“第N集”时，链接增加强制参数并优先打开指定集，随后页面地址恢复为普通续播状态。旧令牌历史会在首次打开同一资源时迁移到新键名。
- 进度只保存在用户当前浏览器的 `localStorage`，不上传服务器；不同设备、浏览器配置或清除微信网页缓存后不会共享记录。65 项单元测试、Python/JavaScript 语法检查和差异检查通过；插件版本更新至 2.4.0，等待用户手动热重载。

## 生图首选 HostCentral 与自动回退

- `jiang_image` 首选模型切换为 HostCentral `https://api.hostcentral.cc/` 的 `gpt-image-2`，API Key 沿用 `GPT_IMAGE_API_KEY` 环境变量来源，不在页面或日志中显示密钥。
- 保留 GemAI `gpt-image-2-2K` 为第一备用，MiniMax `image-01` 为第二备用；首选地址连接、HTTP 或响应解析失败时按优先级继续尝试已启用模型。
- HTTP 客户端将建连超时限制为 15 秒，同时保留配置的生图总超时（默认 300 秒），避免不可达地址长时间阻塞回退链路；已通过 JSON 配置解析和 Python 语法检查，未启动或重启 AstrBot、微信注入进程。
## Image Plugin Configuration WebUI

- Added AstrBot plugin configuration fields for the preferred, secondary, and tertiary image Base URLs and model names, plus secondary/tertiary enable switches.
- Updated `jiang_image` to accept `AstrBotConfig` and synchronize these fields into the persistent ordered fallback profiles without exposing API keys.
- Validated the schema JSON, Python syntax, and model-manager JavaScript syntax; no AstrBot or WeChat process was started or restarted.
