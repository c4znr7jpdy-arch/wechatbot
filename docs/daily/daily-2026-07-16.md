# 2026-07-16 开发日报

## FreeAPI 每日签到

- 排查每日签到中 `FreeAPI (DGBMC)` 返回未授权的问题，确认调度插件调用 `ai_plugin/ai_plugin/checkin.py` 中的站点配置。
- 更新已失效的 FreeAPI 登录 session，保留用户 ID 与现有签到接口不变。
- 使用 2026-07 月度签到查询接口验证新登录态：HTTP 200，响应 `success: true`。
- 未启动、停止或重启 AstrBot、`Py/main.py`，也未手动触发签到 POST 请求。

## 遗留事项

- session 仍属于会过期的网页登录凭据；再次出现 Unauthorized 时需要重新登录并更新。

## 燕云任务消息与累计签到修复

- 将鹅毛任务结果和状态按“每日任务、每周任务、累计任务”分段展示，分组之间增加空行，序号跨分组连续，并恢复领取、待完成、失败等状态图标。
- 优化签到结果排版，增加签到、日期、累计天数、奖励和鹅毛积分图标。
- 修复“第 2 天签到仍显示本月累计 1 天”：原逻辑误把 `cumulativeFig.days` 奖励档位当作累计天数，现改为统计签到日历中 `isSign=true` 的日期；日历缺失时才使用已达成奖励档位回退。
- 同步按实际累计天数选择奖励档位，避免“已经签到”查询显示前一档奖励。
- `jiang_yanyun_checkin` 升级至 v2.0.1；10 项离线测试全部通过，新增奖励档位滞后、零签到、签到排版和每日/每周/累计分组回归。
- 未启动、停止或重启 AstrBot 与 `Py/main.py`，未执行真实燕云写请求。

## 机器人语境化插件调用分析

- 排查截图中“定时 KFC 后要求再来点文案却未调用插件”的原因：KFC 仅注册为 `@filter.command("kfc")`，且主动定时消息不会自动进入 LLM 对话历史。
- 新增 `jiang_capability_router` 自然语言能力网关，向模型暴露 `capability_invoke` 和 `capability_catalog` 两个通用工具。
- 接入 KFC、新闻、天气、Epic、油价、燕云动态、B站用户动态七项低风险能力，并统一参数、权限、直发和失败处理。
- 新增按会话持久化的最近动作记录，支持“再来、继续、换一个、再查”等续调表达；默认有效期 10 分钟。
- 显式新意图优先于最近动作；无上下文的普通“文案”请求会先澄清，避免误调 KFC 或普通文案 API。
- `jiang_commands` 成功执行后记录命令动作；`jiang_schedule` 仅在主动发送成功后记录定时动作。
- 完成 16 项离线测试，并通过五个修改模块的 `py_compile` 与独立导入检查。
- 更新需求与验收文档 `docs/requirements/requirements-context-aware-plugin-invocation.md`。
- 使用用户提供的 WebUI 凭据完成登录并触发插件层全量重载；凭据未写入项目文件。
- 运行日志确认 `jiang_capability_router 1.0.0`、`jiang_commands 1.1.0`、`jiang_schedule 1.1.0` 加载成功，`capability_catalog` 与 `capability_invoke` 两个 LLM 工具完成注册。
- Dashboard API 只读核验显示三项插件均为 `activated=true`；定时任务插件重新注册原有 8 项任务，OneBot 适配器恢复连接。
- 未手动启动、停止或重启 AstrBot 与 `Py/main.py`；插件层全量重载期间 AstrBot 自行切换运行子进程 PID。

## AstrBot 模型请求直连修复

- 定位 `APIConnectionError: Connection error`：AstrBot 请求 mimo、Embedding 和其他回退模型时连接了已失效的本机代理 `127.0.0.1:7890`。
- 清空 `data/cmd_config.json` 中 `google_gemini` 的旧代理配置，当前所有 provider 均未配置代理。
- 调整 AstrBot 生命周期以及 OpenAI 兼容聊天、OpenAI Embedding 与 Gemini HTTP 客户端：全局代理留空时清除 `HTTP_PROXY`、`HTTPS_PROXY`、`ALL_PROXY` 等环境变量，provider 未显式配置代理时设置 `trust_env=False`，避免继承启动终端或系统环境中的代理设置。
- 验证配置 JSON 可解析，修改的 Python 文件可编译；mimo、Embedding 和 freeapi 域名在禁用环境代理时均直连成功并返回未携带密钥时预期的 HTTP 401。
- 未启动、停止或重启 AstrBot 与 `Py/main.py`。

## 盛世活动提醒接口瞬时超时修复

- 定位活动提醒报错为 `GET /api/bot/raid/reminders` 建立 HTTPS 连接时超过 10 秒，并非令牌失效、接口业务错误或响应格式异常。
- 验证生产域名解析及 443 端口正常；使用现有令牌连续调用 10 次均返回 HTTP 200，响应约 0.19–0.78 秒，确认接口当前已恢复。
- 为只读的提醒查询增加一次 1 秒延迟重试，降低单次网络抖动造成的失败；报名写入接口不自动重试，避免重复写入风险。
- 加固错误响应的非 JSON 对象处理，插件版本升级至 v2.1.1；12 项离线测试全部通过。
- traceback 中的 MCP SSE `incomplete chunked read` 属于另一条远端流连接中断，与盛世提醒接口无关。
- 未启动、停止或重启 AstrBot 与 `Py/main.py`。

## 盛世接口直连链路加固

- 收到启用一次重试后仍连续两次 HTTPS 建连超时的运行日志；异常仍发生在 TCP/TLS 建连阶段。
- 对比确认系统 HTTP 客户端成功请求时使用本机 `BaoKeMengCore.exe` 的 `127.0.0.1:7892` 代理，而 `aiohttp` 默认不读取环境代理。
- 新增盛世接口专用 `api_proxy_url` 配置，并在报名、提醒查询和提醒确认请求中显式使用；本机配置为 `http://127.0.0.1:7892`。
- 改进重试警告，输出异常类型和 `repr`，避免 `TimeoutError` 日志内容为空；插件版本升级至 v2.1.2。
- 未启动、停止或重启 AstrBot 与 `Py/main.py`。

## 自然语言油价与燕云热更新修复

- 定位自然语言油价失败并非意图或省份解析错误；`capability_invoke` 已正确传入“四川”，实际故障为 `httpx` 继承失效环境代理后触发 `ReadTimeout`。
- 油价公开 API 改为 `trust_env=False` 强制直连，超时放宽到 15 秒；只读 GET 遇到传输错误时自动重试一次。
- 能力网关、基础命令和定时任务在热更新时显式刷新无状态油价共享模块，避免 `ai_plugin` import 缓存导致修复必须重启进程才能生效。
- 定位燕云旧配置文件带 UTF-8 BOM（`EF BB BF`）；旧配置与旧状态读取改用 `utf-8-sig`，同时兼容有 BOM 和无 BOM JSON。
- 新增油价禁用环境代理、传输错误重试和燕云 BOM 配置回归；能力网关 18 项、燕云 11 项测试全部通过，相关 Python 文件通过编译检查。
- 热更新 `jiang_capability_router`、`jiang_commands`、`jiang_schedule`、`jiang_yanyun_checkin` 成功，四项插件均保持激活；四川油价实网验证返回有效价格与更新时间，热更新新日志中旧配置读取错误为 0。
- 未启动、停止或重启 AstrBot 与 `Py/main.py`。

## 群聊引用消息误唤醒修复

- 定位 14:32 的异常回复：当前消息是 `@cike567 搜一下`，但 11061 引用消息的 `raw_msg` 同时携带了一条曾 @ 姜小妹的旧消息。
- 修复 `Py/main.py` 对引用消息的 @ 判定：先排除 `<refermsg>` 引用块，只从当前消息元数据和显式 `@姜小妹` 正文判断是否唤醒。
- 同时收紧昵称匹配：普通提到“姜小妹”不再被当作 @，必须存在显式 `@`。
- 使用 Python 3.11 32-bit 通过编译检查，并通过 4 项离线回归场景。
- 未启动、停止或重启 AstrBot 与 `Py/main.py`；修复需等下次用户授权重载 `Py/main.py` 后生效。

## 青岛官方潮汐插件

- 确认青岛市官方发布渠道为自然资源部北海预报减灾中心的青岛海洋预报平台，并验证官网逐小时潮位、海水浴场高潮低潮数据接口可直接读取且无需 API Key。
- 新增独立 `jiang_tide` 插件，支持 `/潮汐`、`/明天潮汐`、`/潮汐 石老人`、`/潮汐 金沙滩 明天` 等查询。
- 覆盖第一、第二、第三、第六海水浴场以及石老人、金沙滩、仰口、银沙滩和灵山湾；浴场接口返回精确潮时，青岛沿岸七日数据通过逐小时潮位插值估算潮时。
- 增加当前涨潮/退潮状态、下一次高潮/低潮、十分钟缓存、六小时故障缓存、超时和数据完整性校验。
- 4 项离线测试、Python 编译检查和 AstrBot 插件包导入检查通过；实网验证读到 168 个逐小时潮位点和 9 个浴场预报，命令处理器输出正常。
- 未启动、停止或重启 AstrBot 与 `Py/main.py`，也未执行插件热重载；新插件需在 AstrBot WebUI 完成插件重载后进入运行态。

## 复读机身份提示兼容修复

- 定位 `jiang_repeater` 已启用但不复读：群消息被注入按发言者变化的系统身份提示，插件直接比较完整消息，导致不同用户发送相同正文时仍被判定为不同。
- 比较前移除系统身份提示，只保留真实消息正文；复读输出也使用清理后的正文，避免泄露内部提示。
- 增加复读触发日志，并将插件版本升级至 v1.0.1。
- 4 项离线回归和 Python 编译检查通过；通过 WebUI 插件级热重载成功，日志确认旧处理器已移除并加载 `jiang_repeater 1.0.1`。
- 未启动、停止或重启 AstrBot 与 `Py/main.py`。

## 群分析多日范围展示修复

- 核验 `/群分析 7` 实际按 `days=7` 拉取了 7 天范围数据；旧 `scrapbook` 模板仍固定显示“一天、今日、24H”，造成只分析一天的误解。
- 将分析天数和起止日期写入分析结果并传入报告渲染，单日保留原文案，多日显示“近 N 天”、实际日期范围、“近 N 天话题”和“近 N 天分时活跃轨迹”。
- 图片发送标题同步显示“近 N 天群聊分析报告”，文本回退报告也展示相同范围。
- 插件本地版本升级为 v4.10.6.1。
- 图片、HTML、文本报告及发送提示均已统一为按实际分析天数展示。
- 新增多日范围单元测试与模板渲染检查；插件热重载成功，未重启 AstrBot 或微信注入进程。
