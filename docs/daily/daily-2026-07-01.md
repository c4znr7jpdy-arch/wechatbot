# Daily Report - 2026-07-01

## Music 11214 Send Path Restore

- Corrected the music card send path back to the verified raw XML route: AstrBot now calls the Py bridge OneBot action `send_raw_xml` first, with `send_type=11214`; the localhost debug-control endpoint is only a fallback probe.
- Fixed the regression where the music plugin only used debug-control, which can return HTTP 500 even though the working 11214 path is the OneBot action handled inside `Py/main.py`.
- Added the current event `self_id` to the `send_raw_xml` action payload so aiocqhttp can select the correct reverse WebSocket API client when more than one client/connection is present.
- Kept NetEase NodeJS search as GET query parameters, so different Chinese song names no longer collapse to the same `恶作剧 - 林依晨` result.
- Changed generated NetEase music card XML to use the local NodeJS `/song/url` playable URL as `<dataurl>`, because the stable `outer/url` shape can produce a card but is not reliably playable.
- When the local NodeJS API does not return a playable URL, the plugin now refuses to build a fake playable card instead of falling back to `outer/url`.
- Added the missing `asyncio` import in `astrbot_plugin_music/main.py`, needed when the local NetEase NodeJS API has to auto-start during plugin initialization.

## Verification

- Passed `astrbot_venv\Scripts\python.exe -m py_compile data\plugins\astrbot_plugin_music\main.py data\plugins\astrbot_plugin_music\core\sender.py data\plugins\astrbot_plugin_music\core\platform\ncm_nodejs.py`.
- Verified local NodeJS searches resolve distinct songs: `恶作剧 -> 255574`, `发如雪 -> 2640178002`, `出现又离开 -> 1363553440`.
- Verified generated data for `出现又离开` uses local NodeJS song ID `1363553440`, a real `music.126.net` playable URL from `/song/url`, and a real cover URL.

## WeChat Raw XML 11214

- Recorded DLL API `type=11214` as the CDN raw XML send interface.
- Confirmed request shape is `{"type":11214,"data":{"to_wxid":"...","content":"<appmsg ...>...</appmsg>"}}`.
- Important finding: `content` should be the inner `<appmsg>` XML, not the received outer `<?xml?><msg>...</msg>` wrapper.
- Updated `Py/main.py` so `helper_send_raw_xml` automatically extracts `<appmsg>...</appmsg>` from full received appmsg raw XML before sending with `type=11214`.
- Updated `astrbot_plugin_music` so playable music cards generate inner `<appmsg type=3>` XML directly for `send_raw_xml`.
- Recorded the interface notes in `AGENTS.md`.

## Verification

- Passed `astrbot_venv\Scripts\python.exe -m py_compile data\plugins\astrbot_plugin_music\core\sender.py`.

## Submodule Flattening

- Converted the broken gitlink entries for `Py`, `ai_plugin`, `lib/hok_brain`, `nonebot-plugin-mystool`, `nonebot_plugin_setu`, and `wechatpyapi_test` into normal tracked directories in the main repository.
- Confirmed the repository no longer has `160000` submodule-mode entries after staging.
- Moved the empty nested `.git` directories out to a temporary backup folder before adding the real files.
- Kept ignored generated/runtime files such as virtual environments, logs, `__pycache__`, `.env*`, and root ignored data out of the commit.

## Verification

- Ran `git ls-files -s` and confirmed no remaining `160000` entries.

## Qishui Music Source Probe

- Captured WeChat `type=11054` appmsg raw XML into `Py\data\appmsg\last_11054_raw.xml` / `.json`, alongside the existing 11061 appmsg capture path.
- Confirmed Qishui's received card is a share card (`<type>5</type>`) and does not contain `<dataurl>` directly.
- Added `QishuiMusic` as a music platform that accepts a Qishui share URL, `/qishui/song/<track_id>`, or raw `track_id`, fetches the share page, parses `_ROUTER_DATA.audioWithLyricsOption`, and resolves the full playable audio URL plus cover/title/artist/duration.
- Registered `汽水点歌`, `qs点歌`, and `qishui点歌` triggers.
- `点歌 <汽水分享链接>` now auto-selects the Qishui player; explicit Qishui commands no longer silently fall back to NetEase when parsing fails.
- Card sending now supports Qishui by filling the existing WeChat music `<appmsg type=3>` template and sending it through the existing 11214 raw XML path.
- Investigated Qishui pure keyword search. The share H5 exposes track detail/player APIs (`/luna/h5/seo_track`, `/luna/h5/card`) but not a usable public search endpoint in the loaded bundle, so pure `汽水点歌 歌名` is intentionally not enabled yet.

## Verification

- Passed `astrbot_venv\Scripts\python.exe -m py_compile data\plugins\astrbot_plugin_music\core\platform\qishui.py data\plugins\astrbot_plugin_music\core\platform\__init__.py data\plugins\astrbot_plugin_music\core\sender.py data\plugins\astrbot_plugin_music\main.py Py\main.py`.
- Verified Qishui track `7634441185327745025` resolves to `雨过后的风景 - Dizzy Dizzo (蔡诗芸)`, duration `243133ms`, with a `douyinvod.com` audio URL and cover art URL.

## NetEase Trial Source Filtering

- Added local filtering for NetEase NodeJS sources so 30s/45s trial URLs are no longer treated as playable full songs.
- `NetEaseMusicNodeJS.fetch_extra()` now tries `/song/url/v1` with `exhigh -> higher -> standard`, falls back to `/song/url`, and rejects sources with `freeTrialInfo.end`, trial-only privilege flags, obviously short `time`, or tiny file size.
- Single-song mode now fetches at least 5 NetEase candidates and calls `fetch_extra()` on each until a full playable URL is found, instead of blindly sending the first search result.
- Added the same trial-source rejection to the plain NetEase Web API path as a guard for explicit `网易点歌`.

## Verification

- Passed `astrbot_venv\Scripts\python.exe -m py_compile data\plugins\astrbot_plugin_music\core\platform\ncm_nodejs.py data\plugins\astrbot_plugin_music\core\platform\ncm.py data\plugins\astrbot_plugin_music\main.py`.
- Verified NetEase NodeJS rejects `1363553440` because `freeTrialInfo.end=45`.
- Verified NetEase NodeJS accepts `2668409804` with `level=exhigh`, `time=180705`, and `size=7230765`.
- Verified single-song selection skips `1363553440` and picks `2668409804`.

## Music Card Direct Send Restore

- Diagnosed the 14:19 group `点歌` failures: `send_raw_xml` / `wechat_link_card` exceptions were logged with empty messages, and the music card path was only trying the AstrBot/OneBot action before falling back.
- Updated `astrbot_plugin_music` card sending to try the local Py debug-control endpoint `http://127.0.0.1:18766/send_raw_xml` first with `send_type=11214`, then fall back to OneBot `send_raw_xml`.
- Improved exception logging for music send failures with `logger.exception(... {e!r})`, so future `ApiNotAvailable`/transport errors include a stack trace instead of an empty suffix.
- Verified the local Py control endpoint health check returns `{"ok":true,"connected":true}`.

## Verification

- Passed `astrbot_venv\Scripts\python.exe -m py_compile data\plugins\astrbot_plugin_music\core\sender.py data\plugins\astrbot_plugin_music\main.py`.

## Music Card For NetEase No-URL Results

- Diagnosed `点歌 恶作剧`: NetEase search returned the correct first result `恶作剧 - 林依晨`, but `song/enhance/player/url` returned no playable URL, so the plugin replied with text before entering the card send path.
- Updated `astrbot_plugin_music` so NetEase card mode still builds a WeChat `<appmsg type=3>` card when `song.audio_url` is empty, using `https://music.163.com/song/media/outer/url?id=<song_id>.mp3` as the `dataurl` fallback.
- Kept the first search result instead of replacing exact matches with later playable same-name covers.

## Verification

- Passed `astrbot_venv\Scripts\python.exe -m py_compile data\plugins\astrbot_plugin_music\main.py data\plugins\astrbot_plugin_music\core\sender.py`.
- Verified generated XML for `恶作剧 - 林依晨` contains `<type>3</type>` and `<dataurl>https://music.163.com/song/media/outer/url?id=255574.mp3</dataurl>`.

## Local NetEase NodeJS API Deployment

- Deployed a local `NeteaseCloudMusicApi` service under `services/netease-cloud-music-api`, pinned to npm package version `4.32.0`.
- Started the service on `http://127.0.0.1:3300`; the running listener is on local port `3300`.
- Added `scripts/start-ncm-api.ps1` for repeatable startup and documented the service in `services/netease-cloud-music-api/README.md`.
- Switched `data/config/astrbot_plugin_music_config.json` to use `default_player_name="nj点歌"` and `nodejs_base_url="http://127.0.0.1:3300"`.
- Updated `AGENTS.md` startup instructions from two processes to three processes so the local NetEase API sidecar is started before AstrBot.
- Note: `npm install` reported 3 upstream dependency vulnerabilities in `NeteaseCloudMusicApi`; left them unchanged to avoid a breaking forced upgrade.

## Verification

- Passed `astrbot_venv\Scripts\python.exe -m py_compile data\plugins\astrbot_plugin_music\main.py data\plugins\astrbot_plugin_music\core\sender.py data\plugins\astrbot_plugin_music\core\platform\ncm_nodejs.py`.
- Verified `GET http://127.0.0.1:3300/song/url?id=255574` returns `code=200` with a direct `music.126.net` mp3 URL.
- Verified `NetEaseMusicNodeJS.fetch_extra()` now resolves `恶作剧 - 林依晨` to a non-empty `song.audio_url` through the local service.

## Local NetEase API Auto-Start

- Updated `astrbot_plugin_music` to auto-start the local `NeteaseCloudMusicApi` sidecar during plugin initialization when `nodejs_base_url` points to `127.0.0.1` or `localhost` and `/login/status` is not responding.
- The plugin starts `npm.cmd start` in `services/netease-cloud-music-api` with `PORT=3300`, detached from the AstrBot console, and logs output to `services/netease-cloud-music-api/service.log`.
- Kept `scripts/start-ncm-api.ps1` as a manual troubleshooting entry, but it is no longer required for normal startup.
- Updated `AGENTS.md` and the service README to document the auto-start behavior.
- Added a service `.gitignore` so `node_modules/` and `service.log` stay local runtime artifacts.

## Verification

- Passed `astrbot_venv\Scripts\python.exe -m py_compile data\plugins\astrbot_plugin_music\main.py data\plugins\astrbot_plugin_music\core\sender.py data\plugins\astrbot_plugin_music\core\platform\ncm_nodejs.py`.
- Verified `GET http://127.0.0.1:3300/login/status` returns `{"data":{"code":200,...}}`.

## Music Duplicate Card Fix

- Diagnosed repeated `恶作剧 - 林依晨` cards after requesting different songs: one cause was the local NodeJS search bug below; additionally, the Py debug-control `send_raw_xml` path can return `{"ok": false}` even when WeChat has already emitted a music appmsg card.
- Restored the music plugin's normal card path to raw XML `type=11214` as required, but made it a single fire-and-forget path: `ok=false` is treated as already handed to the DLL, and the plugin does not fall back to link/file/voice sends that can duplicate or confuse results.
- Normal 点歌 now avoids the noisy `网易云音乐卡片通过 Py 调试控制端口发送失败: HTTP 500` error log while still using raw XML `11214`.
- Diagnosed the local `NeteaseCloudMusicApi` search issue: the NodeJS source used POST form data for `/search`, which the local service did not handle correctly, causing different Chinese keywords to return the same result.
- Updated `NetEaseMusicNodeJS.fetch_songs()` to call `/search?keywords=...&limit=...&type=1&offset=0` with URL-encoded GET parameters.

## Verification

- Passed `astrbot_venv\Scripts\python.exe -m py_compile data\plugins\astrbot_plugin_music\core\platform\ncm_nodejs.py data\plugins\astrbot_plugin_music\core\sender.py data\plugins\astrbot_plugin_music\main.py`.
- Verified local NodeJS searches now resolve distinct results:
  - `恶作剧 -> 255574 恶作剧 - 林依晨`
  - `发如雪 -> 2640178002 发如雪 (女声版) - 余不不`
  - `出现又离开 -> 1363553440 出现又离开 (Live) - 梁博`
- Verified generated raw XML titles also differ for the same three songs.

## Music Single-Mode Regression Fix

- Fixed a regression introduced by playable-source filtering: single-song mode searched multiple candidates to skip unavailable songs, but then kept all playable candidates and entered the selection-list branch.
- This caused WeChat runs to try sending a text selection list through unavailable OneBot actions instead of directly sending the music card.
- Single-song mode now keeps only the first playable candidate and immediately sends it as before.

## Verification

- Passed `astrbot_venv\Scripts\python.exe -m py_compile data\plugins\astrbot_plugin_music\main.py data\plugins\astrbot_plugin_music\core\sender.py`.

## Music Raw XML Direct Fallback

- Diagnosed continued `网易云音乐卡片发送失败` after the single-mode fix: the flow reached card sending, but `event.bot.api.call_action("send_raw_xml")` was unavailable in the current aiocqhttp API transport.
- Added a direct localhost fallback in `astrbot_plugin_music`: if OneBot `send_raw_xml` fails, the plugin POSTs the same `<appmsg>` payload to the Py injection layer debug-control endpoint `http://127.0.0.1:18766/send_raw_xml` with `send_type=11214`.
- Changed the music-card send order to prefer the Py debug-control endpoint first, then OneBot `send_raw_xml`, avoiding a misleading red error log before the working local path is tried.
- Verified the Py control endpoint is alive and connected via `GET http://127.0.0.1:18766/health`, returning `{"ok":true,"connected":true}`.

## Verification

- Passed `astrbot_venv\Scripts\python.exe -m py_compile data\plugins\astrbot_plugin_music\core\sender.py data\plugins\astrbot_plugin_music\main.py`.

## Music Plugin Rollback To Cover Fix

- Rolled `astrbot_plugin_music` back to the cover-fix state after card sending regressions.
- Kept NetEase cover completion via `song/detail` and kept `<songalbumurl>` / `<thumburl>` in the music appmsg XML.
- Removed the later playable-only candidate filtering path, the single-mode multi-candidate behavior, the `send_msg` selection-list fallback, and the direct `debug-control` raw XML send path from the music plugin.

## Verification

- Passed `astrbot_venv\Scripts\python.exe -m py_compile data\plugins\astrbot_plugin_music\main.py data\plugins\astrbot_plugin_music\core\sender.py data\plugins\astrbot_plugin_music\core\platform\ncm.py data\plugins\astrbot_plugin_music\core\platform\ncm_nodejs.py`.
- Passed `C:\Users\Administrator\AppData\Local\Programs\Python\Python311-32\python.exe -m py_compile Py\main.py`.

## NetEase Music Cover Art

- Fixed missing cover art on playable NetEase music cards.
- `NetEaseMusic.fetch_songs()` and `NetEaseMusicNodeJS.fetch_songs()` now preserve cover URLs when the search provider returns `al.picUrl`, `album.picUrl`, or similar fields.
- `NetEaseMusic.fetch_extra()` now calls NetEase official `song/detail` to fill `al.picUrl` without relying on the third-party meting/qijieya endpoint.
- `NetEaseMusicNodeJS.fetch_extra()` also tries NodeJS `/song/detail` when `song.cover_url` is still empty.
- Music appmsg XML now writes both `<songalbumurl>` and `<thumburl>` with the resolved cover URL.

## Verification

- Passed `astrbot_venv\Scripts\python.exe -m py_compile data\plugins\astrbot_plugin_music\core\platform\ncm.py data\plugins\astrbot_plugin_music\core\platform\ncm_nodejs.py data\plugins\astrbot_plugin_music\core\sender.py`.
- Verified NetEase `song/detail` returns real cover URL for `出现又离开`: `https://p2.music.126.net/1RV1U7jfY8MOWYuGsL6KnA==/109951173461278238.jpg`.

## NetEase Playability Check

- Confirmed NetEase search/detail can return song metadata even when no playable source is available.
- `NetEaseMusic.fetch_extra()` now calls NetEase `song/enhance/player/url` and only sets `song.audio_url` when the API returns a real URL.
- `astrbot_plugin_music` no longer sends a playable-looking appmsg card when `song.audio_url` is empty; it replies that NetEase has no playable source for that song instead.

## Verification

- Passed `astrbot_venv\Scripts\python.exe -m py_compile data\plugins\astrbot_plugin_music\core\platform\ncm.py data\plugins\astrbot_plugin_music\core\sender.py`.
- Verified `出现又离开` ID `2668409804` returns `code=200` and a playable URL.
- Verified `月亮之上` ID `354824` returns `code=-110` and `url=null`.

## Playable-Only Song Selection

- `点歌` now searches multiple candidates in single-song mode instead of trusting the first metadata result.
- Before showing a selection or sending a card, the plugin calls `fetch_extra()` and keeps only songs with a real `audio_url`.
- This skips results like NetEase `出现又离开 (Live)` ID `1363553440` and `月亮之上` ID `354824`, which have metadata but no playable URL.
- Investigated Qishui/Soda Music (`beta-luna.douyin.com` / `api5-lq.qishui.com`) as a future source. Publicly discoverable endpoints confirm the service exists, but the search parameter shape still needs capture from the app or a known working request before enabling it as a first-class source.

## Verification

- Passed `astrbot_venv\Scripts\python.exe -m py_compile data\plugins\astrbot_plugin_music\main.py data\plugins\astrbot_plugin_music\core\platform\ncm.py data\plugins\astrbot_plugin_music\core\sender.py`.
- Verified NetEase search candidates for `出现又离开`: first result has `url=null`, second result has a playable URL.
- Verified NetEase search candidates for `月亮之上`: first result has `url=null`, later candidates have playable URLs.

## Random Image Recall

- Diagnosed `图来` auto recall failure: `auto_recall_last_msg` required the image send receipt to already be in the recall queue when the action was registered.
- Fixed the race between AstrBot image sending and WeChat `11047` send receipt handling.
- `auto_recall_last_msg` now schedules even when the queue is still pending, and the delayed recall re-checks the queue by `msg_type="image"` before calling `type=11244`.
- Kept type filtering so delayed recalls do not accidentally recall text or card messages from the same chat.
- Moved `图来` recall scheduling before `yield event.image_result(...)`, because AstrBot may not resume plugin code after yielding the image result.
- The delayed recall worker now retries when the `11047` send receipt has not reached the queue yet.

## Verification

- Passed `C:\Users\Administrator\AppData\Local\Programs\Python\Python311-32\python.exe -m py_compile Py\main.py`.
- Passed `astrbot_venv\Scripts\python.exe -m py_compile data\plugins\jiang_commands\main.py`.

## Voice Raw XML Probe

- Captured WeChat voice `raw_msg` from `type=11048`; the payload is `<msg><voicemsg ... /></msg>` with `aeskey`, `voiceurl`, `length`, `voicelength`, and `clientmsgid`.
- Added a localhost-only debug control endpoint in `Py/main.py` for direct send probes without going through AstrBot action routing.
- Probed `type=11214` with the full `<msg><voicemsg ... /></msg>` payload: DLL accepted the send call, but no `11047` send receipt was observed.
- Probed `type=11044` with the full voice XML: DLL send returned `False`.
- Probed `type=11214` with bare `<voicemsg .../>`: DLL send returned `False`.
- Current conclusion: current DLL raw XML paths do not appear to send WeChat voice bubbles from `voicemsg` XML alone; voice sending likely needs a real voice/media send path or upload-generated media metadata.

## Proactive Chat LLM Fallback

- Diagnosed proactive message failures caused by `deepseek/deepseek-v4-pro` returning HTTP 402 `Insufficient Balance`.
- Found normal AstrBot chat runner has provider fallback handling, but `astrbot_plugin_proactive_chat` called only the current provider plus the same provider's legacy API path.
- Updated `data/plugins/astrbot_plugin_proactive_chat/core/llm_adapter.py` to try the current chat provider first, then `provider_settings.fallback_chat_models` in order.
- The proactive plugin now logs provider switch attempts and only reschedules after all configured providers fail.

## Verification

- Passed `astrbot_venv\Scripts\python.exe -m py_compile data\plugins\astrbot_plugin_proactive_chat\core\llm_adapter.py`.

## Music Selection Send Fallback

- Diagnosed `Task exception was never retrieved` from `MusicSender.send_song_selection()`: the song selection list was sent in a background task and `send_group_msg` raised `ApiNotAvailable`.
- Updated `astrbot_plugin_music` selection-list sending to try standard OneBot `send_msg` first, then fall back to `send_group_msg` / `send_private_msg`.
- Wrapped the background selection-list send path so API failures are logged instead of leaking as unretrieved task exceptions.

## Verification

- Passed `astrbot_venv\Scripts\python.exe -m py_compile data\plugins\astrbot_plugin_music\core\sender.py`.
