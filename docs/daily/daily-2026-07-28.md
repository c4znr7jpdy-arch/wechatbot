# 2026-07-28 开发日报

## 主动消息重复话术修复

### 问题

- 群聊主动消息约有一半在重复“暑假都在耍啥巴适游戏”一类话术。
- 平台聊天流水包含 Bot 自己的旧消息，但配置仅用 `bot` 识别 Bot，实际微信发送者的 wxid 和昵称未被识别。
- 提示词要求优先延续最近话题，导致旧主动消息被模型当成新的仿写样本。
- 插件没有发送前的相似度检查。

### 修复

- 从会话状态自动加入 Bot 的 `self_id`，并将“姜小妹”加入 Bot 标识。
- 平台历史由 200 条缩减为 40 条，降低陈旧话题持续污染上下文的概率。
- 在群聊平台流水提示词中明确：`Bot` 历史只用于避重，不得复述、近义改写或只替换语气词。
- 持久化每个会话最近 20 条主动消息，并从现有平台流水补齐旧会话的避重样本。
- 生成前将最近 8 条话术作为禁止复用清单注入提示词。
- 发送前使用规范化文本、序列相似度和二元字符集合相似度进行硬检查，阈值为 `0.72`。
- 命中重复后最多立即重新生成 2 次；三版都未通过时拦截本轮并重新调度。
- 会话键合并时同步合并避重历史，避免重启后的规范化流程丢失记录。

### 验证

- 三个修改后的 Python 模块均通过 `py_compile`。
- 配置 JSON 可正常解析。
- 模拟验证了微信 Bot wxid 识别、平台历史样本补齐、近似话术命中、自动重新生成成功和持久化列表合并。
- 已在 AstrBot 管理页单独重载 `astrbot_plugin_proactive_chat`；日志确认插件成功终止、重新加载并恢复 3 个既有定时任务。
- 未启动、停止或重启 AstrBot 与 `Py/main.py`。
