# 2026-07-19 开发记录

## 米游币任务人机验证处理

- 确认“签到触发人机验证”属于米游社风控，不是账户登录失效或普通网络错误。
- 为米游币任务结果增加 `requires_user_action` 标记。
- 任务接口返回需要验证时，结果中明确提示用户打开米游社 App 手动完成“打卡”。
- 每日自动任务遇到人机验证后立即停止本轮补跑，不再每 2 分钟连续重试，避免重复触发风控。
- 普通未完成任务仍按原规则每 2 分钟重试，最多 5 次。
- 插件版本更新为 `2.7.1`。

## 验证

- Python 语法编译检查通过。
- 模拟人机验证结果确认只执行 1 次任务并发送可操作提示，不会进入后续 2 分钟补跑。

## 运行说明

- 本次未启动、停止或重启 AstrBot 与 `Py/main.py`。
- 加载插件改动只需重启 AstrBot，`Py/main.py` 无需重启。
