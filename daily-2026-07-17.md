# 2026-07-17 开发日报

## 燕云定时个人通知恢复

- 排查早间收到“燕云多人任务汇总、账号总数 0”且没有个人结果的问题，确认多人数据库为空，同时 WebUI 配置已不再保留旧 `access_token` 与 `token_source_file`，导致首次管理员迁移没有账号可写入。
- 从现有旧抓包安全提取管理员 JWT，仅调用签到状态接口完成只读验证；Token 有效期至 2026-09-13，随后使用既有 Fernet 密钥加密写入多人数据库。
- 恢复固定管理员 `fengchenhao002` 的绑定、私聊通知目标和旧 `state.json` 状态；数据库现有 1 个已启用、已开启通知、具备通知目标的绑定账号。
- 移除定时 Cron 的管理员多人汇总和全局异常私聊，早间批处理现在只向每个绑定用户发送各自的签到与鹅毛任务结果；零账号仅写脱敏日志。
- `jiang_yanyun_checkin` 升级至 v2.0.2；12 项离线测试全部通过，新增“Cron 不发送管理员汇总”回归测试。
- 未启动、停止或重启 AstrBot 与 `Py/main.py`。本地 AstrBot WebUI 未登录，按安全规则未绕过认证，因此代码仍需在 WebUI 中执行一次燕云插件级重载后生效。

## 青岛潮汐卡片排版

- 将 `/潮汐` 与浴场潮汐结果由容易折行的纯文本改为 840px 图片信息卡。
- 顶部集中显示地点、日期、当前涨退潮状态与下一次潮时；高潮、低潮分栏展示独立时间和潮位。
- 底部保留官方数据来源、插值估算说明和缓存/浴场降级提示；图片渲染异常时自动退回原文本。
- `jiang_tide` 升级至 v1.1.0；4 项离线测试、Python 编译检查及真实官方数据渲染预览均通过。
- 仅执行插件级热重载，未启动、停止或重启 AstrBot 与 `Py/main.py`。

## 潮汐普通聊天误触发修复

- 根据 09:11 日志确认，普通赶海聊天中的“等退潮时候”被潮汐插件错误识别，移除“退潮”后又将整段消息误当成浴场名。
- 潮汐查询改为仅响应 `/` 或 `\` 前缀；潮汐关键词还必须位于命令起始位置，避免 `/赶海要等退潮吗` 等 AI 问句被截获。
- 新增真实误触发原句、无前缀潮汐文本和斜杠赶海问句回归测试；插件升级至 v1.1.1。

## 潮汐原生命令接管修正

- 定位 `/潮汐 金沙滩` 未触发：AstrBot 唤醒阶段已移除 `/`，v1.1.1 在插件内二次校验前缀，错误拒绝了合法命令。
- 处理器改用 AstrBot 原生 `@filter.command` 注册潮汐命令及日期/涨退潮别名，由框架可靠区分命令与普通聊天。
- 解析器新增仅供原生命令处理器使用的唤醒后正文模式；外部默认解析仍要求显式前缀，普通聊天保持不触发。
- 插件升级至 v1.1.2。
- 5 项回归测试与 Python 编译检查通过；插件级热重载成功，日志确认 v1.1.2 已加载，未重启任何进程。

## 群分析总结插件 v4.10.9 微信适配

- 将 `astrbot_plugin_qq_group_daily_analysis` 从本地兼容版 v4.10.6.1 合并升级到 v4.10.9.1，保留微信头像、昵称、多日报告时间范围等既有补丁。
- 合入上游 v4.10.7-v4.10.9 的 LLM 重试与降级补偿、禁言检测修复及 BlueArchive（蔚蓝档案）模板。
- 针对微信 OneBot 桥接增加禁言检测旁路，避免上游 QQ 逻辑把 `xxx@chatroom` 群号强制转换为整数而导致分析失败。
- 修复模板选择兼容性：配置面板加入 BlueArchive，模板命令会清理首尾空格并支持大小写不敏感名称；序号选择和配置持久化回读均已验证。
- 下载并补齐 `BlueArchive-demo.jpg`，`/查看模板` 可展示新模板预览。
- Python 全量编译、9 套模板发现、BlueArchive 七个 Jinja 模板加载、配置保存/回读、微信群禁言旁路及 BlueArchive 调试渲染均通过。
- 未启动、停止或重启 AstrBot 与 `Py/main.py`，也未修改现有插件配置；代码需在 WebUI 中执行一次群分析插件级重载后生效。

## AstrBot 4.26.6 升级

- 将 `astrbot_venv` 中的 AstrBot 从 v4.26.1 更新至官方最新稳定版 v4.26.6，保留现有 `data/cmd_config.json`、业务插件和微信注入层逻辑。
- 重新迁移两处 aiocqhttp 微信兼容补丁：非数字 `wxid` / `xxx@chatroom` 会话发送支持，以及 `@` 消息中的非数字 wxid 解析支持。
- 升级前后对 5,431 个业务源码与配置文件计算聚合 SHA-256，结果一致，确认现有业务逻辑未被改动；AstrBot 包与官方 wheel 对比仅上述两个兼容文件存在预期差异。
- AstrBot 版本/导入、CLI、Python 语法、依赖完整性及非数字群聊/私聊会话发送冒烟测试均通过。
- 未启动、停止或重启 AstrBot 与 `Py/main.py`；当前运行实例继续使用已加载代码，v4.26.6 将在后续明确授权重启 AstrBot 后完整生效。
