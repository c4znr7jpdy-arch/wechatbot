# Daily Report 2026-07-24

## GemAI Image Generation and WebUI

- Reworked `data/plugins/jiang_image` around a runtime-configurable model manager.
- Configured GemAI `gpt-image-2-2K` as the active default image model.
- Configured the GemAI 2K profile to send aspect ratios through the `size` field and accept the default `b64_json` response, matching the gateway's synchronous Images API contract.
- Stored the supplied GemAI credential only in the ignored runtime file `data/plugin_data/jiang_image/models.json`; source files, WebUI responses, logs, and this report do not contain the credential.
- Kept the previous GPT Image endpoint and MiniMax `image-01` as ordered fallback profiles.
- Added support for three custom backend protocols:
  - OpenAI-compatible `/images/generations` and `/images/edits`
  - OpenAI-compatible `/chat/completions` image generation/editing
  - MiniMax `/image_generation`
- Added configurable endpoint paths, authentication source, size parameter mode, timeouts, proxy inheritance, response format, extra headers/body, editing support, enable state, and fallback priority.
- Added atomic JSON persistence and runtime hot reload. Saving a model or changing the active model affects the next request without restarting AstrBot.
- Added safe API Key sources: plugin-local secret, environment/`.env.prod`, or an existing AstrBot Provider. Page responses expose only a masked key and presence flag.
- Added generic image result extraction for URL, data URI, base64, and Markdown image responses.
- Added a Plugin Page at `pages/model-manager` for adding, editing, deleting, testing, enabling, selecting, and reloading generation models.
- Updated plugin metadata/i18n and project documentation for the new WebUI and model-ID switch command.

## Verification

- Confirmed the configured GemAI credential can access `GET /v1/models` with HTTP 200.
- Confirmed GemAI returns `gpt-image-2-2K` in the available model list.
- Did not submit a real image generation request, avoiding an unrequested billable call.
- Compiled `main.py` and `model_manager.py` with the AstrBot Python 3.13 environment.
- Imported `jiang_image.main` successfully.
- Instantiated the plugin with a fake context and verified all six Plugin Page API routes register.
- Verified the active model is `gemai-gpt-image-2k`, the fallback chain is ordered, and public config contains no `api_key` field.
- Exercised add/save/select/reload behavior in an isolated temporary configuration and verified stored secrets remain redacted from public responses.
- Parsed plugin/runtime JSON and YAML metadata successfully.
- Checked `pages/model-manager/app.js` with `node --check`.
- Rendered and visually inspected the Plugin Page layout in headless Chrome.

## Process Note

- AstrBot and `Py/main.py` were not started, stopped, restarted, or reloaded.
- The new Page and backend code require one manual `jiang_image` plugin reload before first use; model changes made inside the Page are hot-applied afterward.
