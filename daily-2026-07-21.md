# 2026-07-21 开发记录

## 燕云自动任务调度核验

- WebUI 配置已保存为每天 `23:30`。
- 当前运行中的 AstrBot 已重新注册内存 Cron：`30 23 * * *`。
- 调度时区为 `Asia/Shanghai`，任务处于启用状态。
- 核验时下一次执行时间为 `2026-07-21 23:30`（北京时间）。
- 本次仅进行只读核验，未重启、停止或重载 AstrBot 与微信注入进程。

## 服务器状态插件

- 新增 `jiang_server_status`，支持 `/服务器`、`/服务器状态`、`/主机`、`/主机状态` 命令。
- 默认监控当前 Windows 主机，采集 CPU、内存、C/E 盘合计、实时网络速率和运行时长。
- 默认检查 AstrBot `6185`、微信 OneBot 桥接 `6199`、网易云音乐 API `3300` 三个本地端口。
- 使用 AstrBot `template_list` 配置，可继续添加本机监控、HTTP JSON 指标接口或 TCP 在线探针。
- 新增带矢量图标的图片状态卡，包含在线、需关注、离线三种状态，并提供图片渲染失败时的文本降级。
- 补充配置说明、HTTP 指标格式、真实数据预览和 6 项离线单元测试。
- 本次未启动、停止或重启 AstrBot 与 `Py/main.py`，新插件需要通过 WebUI 重载后生效。

### MyViture 云服务器配置

- 根据云控制台信息新增 `MyViture`：中国香港、Ubuntu 22.04、2 核 2 GiB、40 GiB。
- 公网地址仅写入被 Git 忽略的运行时配置，没有提交到插件源码。
- 实测公网 `22`、`80`、`443` 端口可访问，选用 `443` 作为服务器在线状态和连接延迟探针。
- TCP 状态说明不会回显目标主机地址，避免在群聊状态卡中暴露公网 IP。
- 当前服务器未提供系统指标 JSON 接口，因此暂不展示实时 CPU、内存和磁盘使用率；后续可部署指标接口后切换为 HTTP 监控模板。

### 服务器离线报警

- 按要求移除当前 Windows 主机，运行时配置只保留阿里云 `MyViture`。
- 新增后台报警任务：每 30 秒探测一次，连续 3 次失败判定离线，连续 2 次成功判定恢复。
- 离线时私聊管理员 `fengchenhao002`，恢复时发送一次恢复通知；持续离线每 60 分钟重复提醒。
- 新增管理员命令 `/服务器告警状态` 和 `/服务器告警测试`，便于检查任务与推送通道。
- 告警消息不包含公网 IP，避免目标会话中的地址泄露。
- 已通过 AstrBot WebUI 单独重载 `jiang_server_status` 至 `1.1.3`，并使用 `on_plugin_loaded` 覆盖热重载场景；未重启 AstrBot 或微信注入进程。

### SSH 完整指标采集

- 明确 TCP 探针只能提供在线状态和延迟，重启不会产生 CPU、内存、磁盘等内部指标。
- 验证现有 `root` SSH 密钥可非交互登录 `MyViture`，远端 Python 3.10、Nginx、SSH 服务均可用。
- 新增 SSH 指标模板，使用内存脚本只读采集 `/proc`、系统盘、网络速率、运行时长以及 Nginx/SSH 服务状态。
- SSH 强制批处理密钥认证并隐藏 Windows 子进程窗口；超时或认证失败时降级为安全错误，不回显主机地址。
- `MyViture` 已从 TCP 443 探针切换为 SSH 22 完整指标采集，插件版本升级至 `1.2.0`。
- 已通过 WebUI 热重载 `1.2.0`，真实采集到 CPU、内存、磁盘、网络、运行时长及 Nginx/SSH 状态，后台报警任务保持运行。

### 服务器状态改为纯文本

- 按要求取消服务器状态图片，`/服务器` 改为直接发送结构化文本。
- 在线状态按资源、网络、运行、服务、更新时间分区；离线状态只保留原因、采集方式和检查时间。
- 移除运行路径中的 Pillow 图片渲染及依赖，插件版本升级至 `1.3.0`；报警推送继续使用文本。

## 活动结束清空金山表格两列

- 金山 AirScript 的报名同步范围仍为 `B3:B12`，C 列不参与网站名单读写。
- 活动封存成功后的清空范围由 `B3:B12` 扩大为 `B3:C12`，一次写入 10 行两列空值。
- 保留原有安全顺序：先同步最终名单、再封存网站活动，只有封存成功才清空；封存失败时 B/C 两列均保留。
- 新增 AirScript 虚拟表格测试，网站完整测试共 6 项全部通过，JavaScript 语法检查和差异检查通过。
- 线上旧文件备份到 `/var/www/shengshi/.codex-backups/kdocs-roster-sync-before-20260721-173858.js`，新文件与本地 SHA-256 一致，`shengshi` 保持 `online`，未重启网站服务。
- 线上 WPS 定时任务需在 AirScript 编辑器中替换并保存新版脚本后才会采用两列清空逻辑。
